# 재설계 점검표 (내부용, 배포 제외) — 콘텐츠 체계 · 디자인 체계 · 사용 체계

기준 문서: `docs/CONTENT_FORMAT.md` (콘텐츠 양식·용어 사전). 공용 규칙: `assets/site.css` v6, 표시 규칙 코드: `assets/content.js`.
검증 도구: scratchpad 의 Playwright 스크립트(로컬 서버 + 검증용 데이터 `testdata.js` = seed 29곳·28 한줄평 + 검수 사례. 실서비스 데이터에는 쓰지 않음).

## 1. 페이지별 작업표

| 경로 | 목적 | 데이터 | 구조 수정 | 문구 수정 | 기능 검증 | PC(1440) | 모바일(390) | 남은 문제 |
|---|---|---|---|---|---|---|---|---|
| index.html | 시작 화면 — 분위기 전달 + 참여·주요 기능 안내 | site_posts_v (공지·모임 모집만) | 완료(4차) — A 계절 대표 소개(제목·소개·오픈채팅·이용 안내 + 가을 일러스트, PC 38/62 나란히·모바일은 글 다음 세로형) → B 처음 참여한다면(짧은 안내 + 이용 안내 링크)·주요 기능 바로가기 3행 → C 고정 공지 1건·다가오는 모임 ≤2건. 장소 기록 6곳·최근 글 목록·참여 3단계는 뺐음(맛집·소식·이용 안내에 그대로) | 완료 — "함께 보내는 가을" / "밥 한 끼부터 주말 나들이까지, 동네친구들과 함께합니다." / 데이터 없으면 "고정된 공지와 예정된 모임이 없습니다." 한 줄 | contenttest.js(홈 8건), herocheck.js(그림 선택·비율·나란히·실패·로딩 이동), 빈 상태·실패 상태 | 확인 | 확인(768 포함) | - |
| places.html | 검색과 비교가 쉬운 디렉터리 + 지도 | site_places_v(lat·lng 포함) + data/places-curated.json, site_schema_v, 카카오맵 JS SDK(KAKAO.jsKey) | 완료(6차 보완) — 목록은 지역별 묶음(기본)·종류 아이콘·등록자 칩·방문/다시 가고 싶음 표시, 펼치면 요약 칩(종류·지역·가격·방문·영업 종료) + 후기·팁·위치(지도에서 위치 보기·카카오맵)·작성 정보(링크 복사) / 지도 패널(PC 오른쪽 고정·모바일 위 카드, 종류색 핀, 범례, 미지정 수) / 등록 폼에 장소 검색(좌표·링크·주소 채움)·첫 한줄평 작성자 칸 / 운영진 위치 잡기 창 / 필터 "기록한 사람"·선택지 개수·초성 검색 | 완료 — "카카오맵 열기"/"카카오맵에서 검색"/"지도에서 위치 보기", "등록 ○○"/작성자 분리, "지역별 묶음", "다시 가고 싶음 N", "마지막 방문", "링크 복사" | contenttest.js 64건(지도·검색·위치 잡기는 SDK 흉내로), a11ytest.js 12건, curate-check.js | 확인(지도 열림·닫힘, 1080 경계) | 확인 | 서버 마이그레이션 전에는 메뉴·팁 칸이 닫힘(안내). 위치 SQL·카카오 키 전에는 지도·장소 검색·위치 잡기가 숨음. 실제 지도 타일·검색 결과는 이 환경에서 못 봄(dapi.kakao.com 차단) |
| news.html | 제목 중심 게시판 | site_posts_v, site_comments_v, site_schema_v | 완료 — 도구 패널(검색·정렬·종류 탭) → 행 목록([유형] 제목 / 보조 줄 / 상태) → 펼친 글(핵심 안내→사실 항목 표→본문→오픈채팅→반응→동작→댓글). 글쓰기·수정은 같은 모달(유형 먼저 → 유형별 항목 → 제목·본문 → 작성 정보 → 미리보기) | 완료(4차 보완) — 모집 안내문이 실제 항목(서버 형식별)·홈 노출 조건(오늘 이후·마감 아님·2건)·오픈채팅 신청을 그대로 말함. 페이지 소개에서 종류 나열·비밀번호 문구 제거(용도는 작성 폼에) | contenttest.js 21건, a11ytest.js 12건 | 확인 | 확인 | 옛 글(meta 없음)은 제목·본문만 — 채우지 않음 |
| guide.html | 목차가 있는 문서 | 정적 | 유지(이전 단계 문서형). 공용 토큰 적용 | "모임(벙)"·"모임장(벙주)"는 첫 언급(주요 이용 규칙)에만 | guidetest.js 6건 | 확인 | 확인 | - |
| report.html | 기간·기준이 정확한 데이터 도구 | site_reports_v | 유지. 공용 토큰 적용(섹션 제목 24px, 카드 모서리 10) | 유지(선택 기간/발행 기간 전체 표기, '벙'→'모임') | reporttest.js 13건 | 확인 | 확인 | 발행 데이터 없어 예시 데이터로 검증 |
| calculator.html | 입력·결과가 명확한 도구 | localStorage, settle_* | 유지. 4차: 제목 옆 합계·참여·1인 숫자 제거(결과 요약 카드 + 모바일 요약 바에 같은 숫자가 있고 제목 옆은 스크롤하면 사라져 도움이 안 됨). 시작 상태는 저장값이 있으면 저장값, 없으면 예시(예시 표시) — 저장값을 지우지 않음 | "모임장" (괄호 설명 제거) | settle-test.js 9, uitest.js 33, nametest.js 7 | 확인 | 확인 | - |
| settle.html | 정산 확인·입금 | settle_get/set_paid | 유지. 공용 토큰 적용 | 유지 | settleview.js | 확인 | 확인 | - |
| games.html | 게임 목록 | 정적 | 유지 | 유지 | 링크 | 확인 | 확인 | 게임 내부 미변경 |
| game_v2.html / bakery.html | 게임 | 게임 저장 | 미변경(복귀 링크만 이전 단계에서) | - | gameback.js | - | 확인 | - |
| report-admin.html | 통계 발행(운영진) | site_reports | 유지. 공용 토큰 적용 | 유지 | footcheck.js | 확인 | 확인 | 서버 설정 전이라 발행 미검증 |
| 공통 셸 | 메뉴·푸터·탭바·팝업·알림·상태 | - | site.css v6: 행 목록(.rows/.row), 패널(.panel/.is-brand/.is-soft), 사실 표(.kv), 유형 라벨(.type-tag), 폼 오류(.field-error)·미리보기(.form-preview), 모바일 시트(.is-sheet), 상태 줄(.status-line) | 용어 사전 적용 | 건너뛰기 링크, 대화상자 포커스 유지·Esc·복귀, 탭바 가림 없음(a11ytest.js), 가로 넘침 390/768/1440 없음(shoot768.js) | 확인 | 확인 | 픽셀 대비 측정(보라 팔레트): 맛집·이용 안내·정산·정산 확인 미달 0. 남은 표시는 홈의 두 글자 묶음 제목 '공지'(13px), 소식 모바일의 12px 유형 라벨, 통계의 '표로 보기'와 접힌 표 셀 — 전부 가는 글자의 안티앨리어싱 측정 오차이며 계산 대비는 5.4~10.7:1 |

## 2. 콘텐츠 종류별 정리 양식 (요약 — 전문은 CONTENT_FORMAT.md)

- 맛집: 식당명 · 지역(+위치 메모) · 종류 · 1인 가격 · 추천 메뉴 · 한줄평 · 방문 팁 · 작성자 · 등록일/방문일 · 지도. 목록은 1~6 순서, 상세는 기본 정보→추천 메뉴→방문 후기→방문 팁→위치와 지도→작성 정보.
- 소식: 공지(핵심 안내·적용 대상·적용일/종료일·필요한 행동·상세) / 모임 모집(모임명·날짜·시간·장소·인원·비용·모임장·신청 방법·준비물·상태) / 모임 후기(활동·날짜·장소·본문·링크) / 정보(핵심 내용·출처·링크·유효 기간) / 자유(제목·본문). 저장은 `site_posts.meta`, 서버 정리기가 아는 키만 남김.

## 3. 기존 게시물 처리

| 대상 | 건수 | 처리 | 못 한 것 |
|---|---|---|---|
| 맛집(옮겨온 29곳) | 29 | 서버 데이터 변경 없음. 표시 규칙만 적용(그 외 지역, 가격 미등록, 위치 메모/주소 구분) | 실서비스 DB 는 이 환경에서 읽을 수 없어 seed 파일(sql/2026-09-21-places-4-seed.sql) 기준. 그 뒤 멤버가 올린 글은 정리본 없이 원문으로 표시됨(정상 동작) |
| 한줄평(옮겨온 28개) | 28 | 정리본 28건(data/places-curated.json) — 4차에서 전부 원문 말투로 다시 씀(자리 나누기 + 맞춤법·띄어쓰기만). 정리 완료 28 / 대기 0 / 메뉴만 있는 글 1(뱅뱅막국수) / 메뉴+팁만 1(무한쌈밥) / 메뉴 칸 비움 3(명태어부·목포명가·더리얼치즈버거). curate-check.js 로 seed 와 완전 일치 확인 | 서버에 쓰지 않았으므로 되돌리기는 파일 항목 삭제로 끝. 운영진이 한줄평 수정으로 저장하면 그때 서버 값이 됨 |
| 소식 | 실서비스 글 수 미확인 | 옛 글은 meta 가 없으면 제목·본문만 표시. 모임 모집의 기존 meta(kind: bung, date/time/place/cap)는 그대로 읽음 | 실서비스 조회 불가 |

정리 전후 비교(대표, 4차 기준 — 3차의 "…입니다" 편집은 폐기):

| 원문 | 3차(폐기) | 4차 추천 메뉴 | 4차 한줄평 | 4차 방문 팁 |
|---|---|---|---|---|
| 덕자찜 레전드집 | 덕자찜 레전드 집입니다. | 덕자찜 | 덕자찜 레전드 집 | — |
| 밥도둑 술도둑 | 밥도둑, 술도둑입니다. | — | 밥도둑 술도둑 | — |
| 간장게장 1티어! 간장게장 안 좋아하는 사람도 정신 놓고 먹어요. 포장도 돼요 | 간장게장 1티어입니다. … | 간장게장 | 간장게장 1티어! 간장게장 안 좋아하는 사람도 정신 놓고 먹어요 | 포장도 돼요 |
| 냉짬뽕 맛집 (매운맛 단계별로 고를 수 있어요) | 냉짬뽕을 추천합니다. | 냉짬뽕 | 냉짬뽕 맛집 | 매운맛 단계별로 고를 수 있어요 |
| 갈비찜이 맛있어요. 냉면도 맛있다는데 저는 그냥 그랬어요 | 갈비찜은 맛있었고, 냉면은 개인적으로 평범했습니다. | 갈비찜 | (원문 그대로) | — |
| 첫 방문은 무조건 더블로! 패티 가장자리 바삭하게 구워진 게 포인트예요 | 메뉴 "더블 치즈버거"(추정) | — (추정 안 함) | 패티 가장자리 바삭하게 구워진 게 포인트예요 | 첫 방문은 무조건 더블로! |
| 제철 신선한 해산물은 여기서! 강남에 다른 지점도 있어요 | 메뉴 "제철 해산물" | — | 제철 신선한 해산물은 여기서! | 강남에 다른 지점도 있어요 |
| 신선한 육회 한상. 요리주점이에요 | 목록 메뉴 줄 + 상세 메뉴 항목 + 후기 아래 메뉴(3회) | 육회 한상 (한줄평에 있어 화면에는 한 번) | (원문 그대로) | — |
| 들기름 막국수 | 들기름 막국수 | 들기름 막국수 | — | — |

## 4. 새 등록·수정 흐름

- 맛집 등록/수정: 식당 이름 → 지역·종류 → 주소 또는 위치 → 지도 링크 → 1인 가격·메모 → (등록 시) 첫 한줄평(선택: 한줄평·추천 메뉴·방문 팁·방문일·1인 금액·재방문) / (수정 시) 영업 종료 → 작성 정보 → 목록 미리보기(첫 한줄평도 목록 규칙 그대로). 저장은 place_create → (한줄평이 있으면) place_note_create 한 번. 한줄평 없이 메뉴·팁만 적으면 한줄평을 요구(가짜 후기 문장 없음). 오류는 항목 아래, 서버 오류(같은 식당·비밀번호·링크·방문일)도 해당 항목에.
- 한줄평 등록/수정: 한줄평(필수) → 추천 메뉴 → 방문 팁 → 방문일 → 1인 금액 → 다시 가고 싶음 → 작성 정보 → 상세 미리보기. 수정은 정리본을 미리 채움. 서버가 이전 형식이면 메뉴·팁 칸을 닫고 안내.
- 소식 글쓰기/수정: 글 종류 → (유형별 항목) → 제목(모집은 모임명) → 내용 → 작성 정보 → 목록 행 + 항목 표 미리보기. 수정도 같은 모달, 항목이 채워짐.
- 우선순위: 작성자가 수정한 서버 값 > 정리본(원문이 같을 때만) > 원문.

## 5. 검증 기록

- 기능(7차 뒤 현재): contenttest.js 98건 · news7test.js 16 · places7test.js 23 · calc7test.js 12 (7차 추가분은 11절) / 이전: contenttest.js 96건(4차에 28건 추가: 검수 사례 4곳·겹침 규칙·정렬 기준·지도 이름·첫 한줄평 흐름·이전 형식 서버·소식 안내문·홈 8건 / 6차에 24건 추가: 지역별 묶음·종류 아이콘·등록자 칩·선택지 개수·초성 검색·사람별 보기·링크 복사·방문 표시·요약 칩, 지도 패널·핀·조건 연동·핀→목록·위치 보기·기억, 장소 검색·고르기·등록자/작성자 분리·저장 순서, 위치 잡기·저장·Esc, 키 없음·좌표 서버 없음), herocheck.js 13건(그림 선택 390@1x/3x·768@2x·1440@1x/2x, 비율, 38/62, 실패 시 글만, 로딩 전후 이동 0px), guidetoc.js(목차 검색창 넘침 없음 3폭), a11ytest.js 24건, uitest.js 33건, settle-test.js 9건, nametest.js 7건, guidetest.js 6건, reporttest.js 13건, footcheck.js, curate-check.js — 전부 통과, JS 오류 없음.
- 화면: 390 / 768 / 1440 가로 넘침 없음(전 페이지). 상태: 빈 목록·불러오기 실패·검색 결과 없음·이전 형식 서버·긴 후기·한 문장 후기·가격 없음·애매한 지역·추천+아쉬움 후기·정보 부족 모임 글·긴 공지·글 2개 목록·마감된 미래 모임·세 번째 미래 모임·공지 없음.
- 홈 그림: 원본 2장 보존, 웹용 4장 생성(헤더로 형식·크기 확인). 모바일 세로형 340×425 vs 가로형 350×233 비교 → 세로형. 계절 블록 하나에서 제목·소개·경로 관리.
- 서버 SQL: 로컬 PostgreSQL 16 에 setup_all → bung-fields → places 1~4 → slash → content-format → places-location 순으로 적용. 두 번 실행 안전, 함수 동작(잘못된 링크 제거, 자유 글 meta 삭제, 옮겨온 한줄평은 운영진만 수정, place_set_location 의 비밀번호·좌표 범위·링크 검사와 주소 보존) 확인.
- 카카오맵: 이 환경은 dapi.kakao.com 에 못 나가므로 SDK 를 같은 이름으로 흉내 낸 `scratchpad/kakao-stub.js` 로 화면 코드(불러오기·지도·핀·검색·위치 잡기)를 검증했다. 실제 타일·검색 결과·키 등록은 배포 뒤 확인해야 한다.
- 직접 확인하지 못한 것: 실서비스 데이터(프록시 차단 — 실서비스에 올라온 글의 실제 표시), 카카오톡 인앱 브라우저·실기기 공유 창, Supabase 실제 적용, 실제 배포 URL 의 그림 로딩, 카카오맵 실제 표시.

## 6. 운영자가 확인할 사실·정책·콘텐츠

1. `sql/2026-09-24-content-format-1-notes.sql` → `-2-meta.sql` → `-3-posts.sql` 세 쪽을 실행해야 추천 메뉴·방문 팁·유형별 항목이 저장됩니다(README 참고. 원래 한 파일이었는데 붙여넣다 잘려 셋으로 나눔). 실행 전에도 사이트는 동작합니다.
2. 정리본(data/places-curated.json)의 문장이 작성자 뜻과 다르다고 판단되면 항목을 고치거나 지우면 됩니다(지우면 원문 표시).
3. 홈 대표 문장 "함께 보내는 가을 / 밥 한 끼부터 주말 나들이까지, 동네친구들과 함께합니다."가 실제 활동 범위와 맞는지(캠핑 전용으로 읽히지 않게 그림과 별개로 식사·나들이를 말함). 계절이 바뀌면 index.html 의 계절 블록만 고침.
4. 소식의 '모집 상태'는 모임장이 직접 바꿉니다. 인원으로 자동 마감하지 않습니다(이전 정책 유지).
5. 영업 종료 식당은 삭제 대신 '현재 영업 종료' 표시가 원칙입니다.
6. 지도: 위치 SQL(`sql/2026-09-25-places-location.sql`)·카카오 앱·Web 도메인·JavaScript 키(`assets/site-core.js` 의 `KAKAO.jsKey`)까지 설정 완료(2026-09-25). 남은 것은 맛집의 '위치 잡기'(운영진 비밀번호)로 옮겨온 29곳의 좌표를 채우는 일. 좌표가 없는 곳은 지도 핀·내 주변에서 빠집니다(sql/README.md).
7. `sql/2026-09-26-settle-guard.sql` 을 실행하면 정산 확인의 입금 완료 표시를 명단에 있는 이름만 받습니다(실행 전에도 동작). 카카오톡 공유·내 주변·홈 화면 추가는 배포 뒤 실기기에서 한 번씩 확인해 주세요.

## 7. 이미지 자리

규격 전문은 `docs/IMAGE_SPEC.md`. 이미지 개수에 맞춰 섹션을 늘리지 않는다. 2026-09-21: 탭별 자리 15곳을 `docs/IMAGE_PLAN.md` 로 기획하고 챗GPT 생성본 16장을 전부 붙였다. 모든 자리는 파일이 없으면 접힌다.

| 자리 | 상태 | 비율 · 표시 크기 |
|---|---|---|
| 홈 대표 그림 | 적용됨 — 원본 `assets/img/src/autumn-desktop.webp`(1536×1024)·`autumn-mobile.webp`(1122×1402), 웹용 `assets/img/autumn-desktop-1440/720.webp`·`autumn-mobile-800/400.webp` | PC 3:2, 글 38%·그림 62%(1440 화면에서 630×420) / 모바일 4:5, 최대 340px(340×425). 잘라내지 않음, 바탕 #F7F5EF, 실패 시 글만 |
| 공유 미리보기(OG) | 적용됨 — `assets/og.jpg`(157 KB), 원본 `assets/img/src/og.html` 캡처. 홈 문구 + 세로형 그림 | 1.91:1, 1200×630, 글자 10~51%·그림 ≤90% |
| 게임 행 캡처 2장 | 자리 없음(파일 오면 같은 방식으로) | 16:9, PC 240×135 / 모바일 350×197, 흰 패널 위 |
| 머리 배너(안내·맛집) | 적용됨 — `guide-banner-1440/720.webp`, `places-banner-1440/720.webp` | 3:2, PC 오른쪽 42% 열 / 모바일 제목 아래 ≤420px |
| 머리 스팟(소식·통계·정산·정산 확인) | 적용됨 — `news-spot`, `report-spot`, `settle-spot` 480/240 | 1:1, 104px(≤640px 화면 76px) 크림 타일 |
| 빈 상태(소식·맛집·통계) | 적용됨 — `news-empty`, `places-empty`, `report-empty` 640/320 | 4:3, 240px |
| 오늘 뭐 먹지 원판 · 오프라인 스팟 | 적용됨 | 64px 원판 · 150px(서비스 워커가 저장) |
| 홈 계절 겨울·봄·여름 | 웹용 파일 준비(`home-<계절>-desktop/mobile-*`), 교체는 수동 | 홈 대표 그림과 같음 |
| 탭별 공유 미리보기 | 적용됨 — `og-guide/places/news/report/settle.jpg`(정산·정산 확인은 settle 공용) | 1200×630 |

## 8. 4차 작업(중복 제거·문구·입력 흐름) 요약

- 뺀 것: 홈 장소 기록 6곳·최근 글 5건·참여 3단계(닉네임 예시·음주·2주) / 정산 제목 옆 합계·참여·1인 / 맛집 소개의 가격 기준 문장·지도 설명 문장 / 소식 소개의 종류 나열·비밀번호 문구 / 목록·상세·후기 아래에 세 번 나오던 추천 메뉴 / 펼친 행의 한줄평 요약(상세와 중복).
- 더한 것: 홈 계절 대표 소개(그림 4장)·참여 경로·주요 기능 바로가기·고정 공지·다가오는 모임 / 맛집 등록의 첫 한줄평(선택) / 소식 모집 안내문의 실제 조건 / `content.js`: `areaText`, `textHas`·`menuItems`·`menusIn`, `noteText`·`noteDate`·`lastNoted`, `showMenuSection`, `placeLine.menuLine`, `kstNowHM`, `isUpcoming`.
- 바꾼 것: 정리본 28건 원문 말투 / "그 외 지역 용산 이태원" → "용산 이태원" / "최근 기록순" → "최근 한줄평순"(방문일 또는 작성일, 없으면 등록일 순) / "지도" ↔ "지도 검색", "지도에서 보기" ↔ "카카오맵에서 검색" / "정리 전 원문 보기" → "원문 보기".
- 고친 결함: 소식 글쓰기·맛집 등록 폼이 390px 에서 두 열로 보이던 문제(display: contents 묶음 안의 넓은 칸에 공용 `.field.is-wide { span 2 }`만 걸려 열이 하나 더 생김) — 페이지 규칙을 자손 선택자 `.field-grid .is-wide` 로 바꿔 한 열로 복귀.

## 9. 색 체계 교체 — 시그니처 보라 (5차)

- 이유: 초록(#285244)과 가을 그림의 주황이 보색에 가까워 부딪혔고, 방장의 시그니처 색이 보라다. 그림을 바꾸지 않고 사이트 색을 옮겼다.
- 팔레트(`assets/site.css` 토큰만 바꿈, 구조·글꼴·간격은 그대로): 바탕 크림 `#F7F5EF`(유지) · 브랜드 보라 `#4B2A82`(이전 판 시그니처 값 그대로, 흰 글자 10.7:1) · hover `#3A2068` · 라벤더 면 `#EDE7F5` · 글자 `#241E2C` / 보조 `#625A6C` · 선 `#E3DFE4` / `#CCC5CF` · 눌린 면 `#EDE9EA`.
- 보라와 짝이 되는 색(작은 자리에만): 녹슨 주황 `#A9502D`(모임 모집 라벨, 맛집 차트), 황토 `#7E5F25`(나들이 차트, 금색 배지 아이콘), 살구 `#E8AD8F`(장식). 크림 바탕·보라·녹슨 주황은 그림의 주황·남색과 분할 보색 관계라 같이 놓여도 튀지 않는다.
- 바뀐 곳: 전 페이지 theme-color·파비콘, 선택 상자 화살표 색, 활동 통계 차트 단계색(보라 5단계)·종류색, 정산 확인 색종이, OG 이미지(재캡처), 푸터·브랜드 패널(보라 위 라벤더 글자).
- 안 바뀐 곳: 그림, 게임 페이지(자체 색), 정산의 영수증·경고 갈색, 위험 빨강.
- 고도화(깊이와 광택, `site.css` 끝의 v7 블록 + 홈·맛집·소식·안내의 페이지 규칙): 페이지 위쪽 라벤더 빛, 반투명 내비(로고 앞 보라 점), 섹션 제목 아래 보라 막대, 주요 버튼의 결·그림자·떠오름, 입력 초점 빛, 라벤더 알약(상태·라벨), 패널 옅은 그림자, 작성자 이름 앞 첫 글자 원(`.who`, 글자 자체는 그대로), 흐르는 스켈레톤, 흐림 배경 모달, 보라→자두 푸터, 하단 탭바 활성 알약. 홈: 계절 알약("2026 가을"), 검정→보라 제목, 그림 뒤 살구·보라 빛과 부드러운 그림자, 기능 바로가기 아이콘. 구조·간격·글자 크기·그림은 그대로. 검증: 콘텐츠 72·접근성 24·정산 UI 33·그림 13건 통과, 가로 넘침 없음, 픽셀 대비 추가 미달 없음.

## 10. 맛집 고도화 — 지도·등록자·검색 (6차)

- 서버: `sql/2026-09-25-places-location.sql` — `site_places.lat/lng`(범위 검사), `place_set_location(id, 비밀번호, 위도, 경도, 지도 링크, 주소)`(등록자 또는 운영진, 좌표 한쪽만 주면 거부, 주소는 비어 있을 때만 채움), `site_places_v` 에 좌표, `site_schema_v` 에 `places_location = 1`. 옮겨온 29곳은 운영진 비밀번호로만. 실행 전에는 지도 기능이 숨고 목록은 그대로.
- 지도(카카오맵 JS SDK): `assets/site-core.js` 의 `KAKAO.jsKey` 가 비어 있으면 아무것도 읽지 않는다. 키와 서버 좌표가 모두 있을 때만 "지도 보기"·"위치 잡기"·장소 검색·"지도에서 위치 보기"가 나타난다. 지도 패널은 PC 오른쪽 400px 고정(목록 행은 두 열로), 모바일은 목록 위 260px. 핀은 종류색 사용자 오버레이(이름 포함), 조건을 바꾸면 핀도 바뀜, 핀 → 목록 펼침, "지도 N곳 · 위치 미지정 M곳", 종류 범례, 켜 둔 상태 기억.
- 좌표 넣기: 등록 폼의 장소 검색(키워드 검색 5건 → 고르면 좌표·지도 링크·주소 채움, 저장은 place_create → place_set_location → place_note_create) / 운영진의 위치 잡기 창(좌표 없는 식당을 차례로 "지역 첫 단어 + 이름"으로 검색, 없으면 이름만, 다른 이름으로 다시 찾기·건너뛰기, 고르면 바로 저장).
- 등록자와 작성자 분리: 목록 행 이름 아래 "등록 ○○" 칩, 한줄평 아래 작성자(첫 글자 원). 등록 폼의 첫 한줄평에 작성자 칸(비우면 등록자 닉네임). 둘 다 누르면 "기록한 사람" 필터. 옮겨온 29곳의 등록자는 운영진 그대로.
- 목록: 기본 정렬 "지역별 묶음"(지역 제목 + 개수, 묶음 안은 최근 한줄평순), 종류 아이콘(8종, `content.js` CAT_ICON/CAT_COLOR — 행·핀·요약 칩이 같은 색), 방문 N회 · 다시 가고 싶음 N(2건 이상) · 마지막 방문일, 필터 선택지마다 개수(0곳은 비활성), 초성 검색("ㅇㅎㄱ"), 필터 "기록한 사람".
- 상세: 요약 칩(종류·지역·가격·방문·영업 종료)으로 기본 정보를 한 줄에, 위치와 지도에 "지도에서 위치 보기" + "카카오맵 열기/카카오맵에서 검색", 작성 정보에 "링크 복사"(#place-N).
- 검증: contenttest.js 96건(지도·검색·위치 잡기는 `scratchpad/kakao-stub.js` 로 SDK 를 흉내 — 이 환경은 dapi.kakao.com 차단), a11ytest.js 24건, uitest.js 33건, 가로 넘침 390/768/1440 없음, 맛집 픽셀 대비 미달 0, JS 오류 없음. 화면: 1440 지도 열림·닫힘, 1080 경계, 390 목록·지도·상세, 등록 폼 검색 결과, 위치 잡기 창.
- 남은 것(운영자): 위 SQL 실행, 카카오 앱·JavaScript 키·Web 플랫폼 등록, 키를 `KAKAO.jsKey` 에 넣고 배포, 위치 잡기로 29곳 좌표 채우기. 실제 지도 타일과 검색 결과는 배포 뒤에 봐야 한다.

## 11. 전면 고도화 — 디자인 v8·홈·안내·통계·정산·소식·맛집 (7차)

목표: 탭마다 "보기 좋다"에서 "쓰인다"로. 도해·시각화·참여 유도·공유를 붙이고, 손이 가는 일(정산 입력, 통계 발행, 후기 작성)을 줄였다. 구조·용어·색 체계(5차)는 그대로.

- 공통(`assets/site.css` v8 블록, `assets/site-core.js`, `assets/content.js`): 숫자 띠(`.stat-strip`), 단계 도해(`.flow`, 가로형 `.is-h`), 콜아웃 4종(`.callout` 기본·주의·위험·팁), FAQ(`.faq`), 그림 자리(`.figure`·`.img-slot`), 카카오 버튼(`.btn-kakao`), 떠 있는 버튼(`.fab`, 모바일만·탭바 위·알림은 그 위로), 카드 링크, 칩(`.chip-fact`), 진행 단계(`.stepper`), 고대비(forced-colors)·인쇄 규칙. `KSHARE.share()`: 카카오 JS SDK 를 처음 누를 때만 읽어 피드 공유 → 못 열면 기기 공유 창 → 링크 복사 순으로 물러섬(결과를 "kakao/share/copy/abort/fail" 로 돌려줌). 닉네임 자동완성(`C.nickSuggest`, datalist). 홈 화면에 추가(PWA): `site.webmanifest`(바로가기 정산·소식·맛집)·`sw.js`(페이지·데이터는 늘 네트워크, `offline.html` 한 장만 저장해 연결이 없을 때 페이지 이동에만 보여 줌 — 다시 시도·홈으로·연결 복귀 감지)·아이콘 4장, 9개 페이지 head 링크. 서비스 워커는 https 에서만 등록. offline.html 을 고치면 `sw.js` 의 CACHE 이름도 올려야 새 판이 저장된다.
- 홈(`index.html`): 참여 흐름 4단계 도해, 주요 기능 4행, 공지·다가오는 모임(2)·최근 후기(1)를 묶음 제목으로, 최근 맛집 기록 3장(마지막 방문 → 최근 한줄평 순), 지난달 활동 통계 요약(4 타일·24시간 막대·핵심 단어 8). 빈 상태는 오픈채팅·소식 버튼 둘, 실패 상태는 다시 시도.
- 이용 안내(`guide.html`): 목차 밖 검색창, 3분 요약(문단 이동 링크), "처음 2주 안내"(가로 단계 도해·닉네임 분해도·체크리스트·운영진 연락 콜아웃), 본문 강조 토큰(`**x**` → 굵게), 정산 두 역할 레인, 금지 행동 3묶음, 판단 흐름, 재입장 카드, FAQ, 해시 이동·스크롤 스파이, 최종 수정일.
- 활동 통계(`report.html`, `report-admin.html`, `assets/chat-parser.js`): 발행 화면 — 끌어다 놓기·여러 파일·기간 자동 감지·겹치는 날짜 정리·나간 사람 감지(앞 토막 매칭 버그 수정)·지난 명단·제목 재사용·잘림(800일·120명) 경고·재집계 디바운스·"자동으로 가져올 수는 없나요?" 설명(오픈채팅은 외부에서 읽는 통로가 없다). 통계 화면 — 히트맵 0건 색 분리·제곱근 눈금, 좁은 화면 차트 축 글자(그릴 때 24 단위·여백 확대, 폭 경계 넘으면 다시 그림), "N일 전 발행". 자동화의 한계: 카카오톡 오픈채팅은 대화 API 가 없어 내보내기 파일이 유일한 입력 — 그래서 '한 달에 한 번 내보내기 → 끌어다 놓기 → 발행' 세 동작으로 줄이는 데 집중했다.
- 모임 정산(`calculator.html`, `sql/2026-09-26-settle-guard.sql`): 진행 단계 표시(멤버 → 비용 → 결과 → 공유), 멤버 칩 편집(빼기 ×·추가 칸·최근 함께한 멤버·자동완성), 확인 필요 항목을 결과 위 콜아웃으로, 카카오톡으로 보내기(입금 확인 링크 자동 생성) + 링크 복사, 복사가 막힌 브라우저용 폴백 창, 예시 데이터가 실제 입력을 덮어쓰기 전 확인. 서버: `settle_set_paid` 가 그 정산의 입금 명단에 있는 이름만 받도록(NOT_IN_LIST).
- 소식(`news.html`): 이번 주 글감 카드(12개 주차별 순환 → 글쓰기 창에 종류·제목 미리 채움), 이번 주 인기 글 3건(최근 7일·반응/댓글 있는 글), 지난 방문 뒤 새 글 점, 지난 모임 글의 '이 모임 후기 쓰기'(활동·날짜·장소·모임 글 링크 채움), 후기의 관련 링크가 이 게시판 글이면 '모임 글 보기' 내부 이동, 상세의 카카오톡 공유·글 접기, 작성 중 임시 저장(localStorage), 빈 상태 3버튼(후기·정보·자유), 모바일 떠 있는 글쓰기 버튼, 댓글 닉네임 자동완성, 글 수 표시를 목록 바로 위로.
- 맛집(`places.html`): 지도 핀 이름표 겹침 접기(우선순위: 펼친 곳 → 방문·한줄평 많은 곳, 확대·축소마다 다시 잼, 점에 마우스·포커스로 이름 표시, 접힌 수 안내)·'전체 보기', 상세의 카카오톡 공유(이름·지역·종류·가격·한줄평), '오늘 뭐 먹지'(지금 조건에서 문 연 곳 무작위 카드, 다시 뽑기는 직전 것 회피, 자세히·닫기), 종류 분포 띠 + 칩(개수, 누르면 그 종류만·다시 누르면 전체, 모바일은 가로 넘김), '내 주변'(기기 위치 → 가까운 순 정렬·행에 거리·지도에 내 위치 점, 권한 없으면 안내하고 정렬 유지, 좌표 서버가 없으면 숨김), 정렬 선택에 '가까운 순'.
- 뒤이어 더한 것: 이용 안내 FAQ '사이트 사용' 5문답(홈 화면 추가·정산 공유·오늘 뭐 먹지·후기 쓰기·임시 저장), 홈 → 맛집 '오늘 뭐 먹지'(#pick), 홈 통계 띠·섹션 제목이 통계 기간에 맞춰 "지난달"/"6월"로, 홈 시간대 막대 설명(role=img), 통계 발행 '자동으로 가져올 수는 없나요?', 떠 있는 버튼 위로 알림, 좁은 화면 차트 축 글자, 고대비·인쇄 규칙, 활동 통계의 카카오톡 공유(발행된 통계만 — 기간·메시지·멤버·활발한 시간 한 줄), 통계 발행 직후 '채팅방에 알릴까요?' 줄(카카오톡으로 알리기·발행된 통계 보기), 오프라인 안내 페이지(offline.html). 이용 안내 검색이 문답(FAQ) 단위로 걸러 펼침(그 절에 맞는 문답이 없으면 전부 표시). 홈에 '이번 주 글감' 줄(소식과 같은 글감 — `content.js` 의 `weeklyPrompt`, `content.js?v=4`) → '글 쓰러 가기'(`news.html#write-prompt`)가 글감이 채워진 글쓰기 창을 바로 연다. 검증 추가: reporttest 15(공유 버튼 숨김/표시·요약), admin7test 4(파일 올리기 → 발행 → 알리기), guide7test 7(사이트 사용 문답·검색 거르기·지우기).
- 검증(전부 통과, JS 오류 없음): contenttest 100 · a11y7test 14(새 요소의 이름·역할·키보드: 추천 카드·종류 칩·핀·내 위치·떠 있는 버튼·공유·숫자 띠·멤버 빼기) · a11ytest 24 · uitest 33 · settle-test 9 · nametest 7(멤버 칩의 × 버튼을 빼고 읽도록 갱신) · guidetest 6 · guidetoc 3폭 · herocheck 13 · reporttest 15 · calc7test 12 · news7test 19 · places7test 24 · guide7test 7 · admin7test 4 · parser7test 11(node)(지도는 `kakao-stub.js` 에 투영·확대 단계·zIndex 를 더해 흉내). 가로 넘침 390/768/1440 없음(8 페이지). herocheck 는 다른 스위트 바로 뒤에 연달아 돌리면 가끔 멈춘다(검증 스크립트 쪽 문제 — 단독 실행은 매번 17초 안에 13건 통과). 픽셀 대비 미달 9~10건은 전부 접힌 요소(details 안 표·목차)와 가는 글자의 안티앨리어싱 — 계산 대비는 통과(host 칩 8.5:1, 축 글자 6.4:1). 화면: 홈 1440/390/빈 상태, 안내 1440/390, 정산 1440/390, 통계 히트맵·차트 390, 소식 1440/390, 맛집 1440(추천 카드·분포·지도)/390/상세, 발행 1440.
- 마지막에 더한 것: 홈 주요 기능의 소식 옆 '새 글 N' 알약(소식 페이지가 남긴 마지막 방문 시각 뒤에 올라온 글 수, 방문 기록이 없으면 표시 안 함), 스크립트 판 번호(`site-core.js?v=5`, `chat-parser.js?v=2`), robots 에 offline.html 제외, 매니페스트 세로 고정 해제.
- 2026-09-21 뺀 것: 홈 커뮤니티 숫자 띠(맛집 수·이번 달 모임·지난달 대화·활동 멤버) — 사용자 요청으로 섹션·`renderStats`·전용 CSS 삭제. `.stat-strip` 은 홈 '지난달 활동' 타일과 통계 페이지가 계속 쓴다. contenttest·a11y7test 는 띠가 없는 것을 확인하도록 바꿈.
- 2026-09-21 레이아웃 검토(103장 캡처, 1440·768·390) 반영 4건: ① 맛집 상세(PC) — 추천 메뉴가 없으면 빈 열이 격자 자리를 차지해 왼쪽 위가 비고 '위치와 지도'가 둘째 줄로 밀리던 것을, 메뉴 열은 있을 때만 내보내고 후기 열(`.pd-col.is-notes`)이 오른쪽 세 줄을 통째로 쓰도록(왼쪽은 메뉴 → 위치 → 작성 정보가 차례로). ② 활동 통계 히트맵(폰) — 24칸 중 12칸만 보이고 옆으로 밀어야 하던 것을 480px 이하에서 24칸을 한 화면에(칸 10px, 마지막 '23' 오른쪽 정렬). ③ 홈(PC) — 주요 기능 카드 넷을 오른쪽 열에서 빼 두 열 아래 전체 폭 한 줄로(`grid-template-areas: "live side" "feat feat"`, `.home-feat`), 카드 그림 `sizes` 를 PC 200px 로. 좁은 화면 순서(참여 흐름 → 주요 기능 → 공지)는 그대로. ④ 맛집 목록(폰) — 가격을 이름 줄 오른쪽, 등록자 칩을 지역 줄에 인라인, 버튼 줄 36px, 한줄평 2줄 → 29곳 9,067px → 6,497px(줄 평균 170px). 카운터 알약 이름은 '오늘·누적' → 'Today·Total'.
- 2026-09-21 방문 카운터: 푸터의 '오늘·누적'을 상단 내비의 알약(`.topnav-visits`, 로고와 메뉴 사이·모바일은 로고와 버튼 사이, 400px 이하는 눈 아이콘 생략)으로 옮기고, 집계를 '기기당 하루 1회'에서 **'링크를 타고 사이트에 들어올 때마다 +1'** 로 바꿈(`visit_hit()`, `site_visit_counts`, `sql/2026-09-21-visit-hits.sql` — 옛 기록은 날짜별 합으로 이어받아 누적이 0 으로 돌아가지 않음). '들어온 한 번' = 탭(세션) 하나가 처음 열릴 때(sessionStorage 표시, 성공했을 때만 기록); 같은 탭 안의 페이지 이동·새로고침은 `visit_ping(p_count=false)` 로 숫자만 읽음. 기기 키 제거. `site-core.js?v=6`, `site.css?v=11`.
- 별도 검토(코드 리뷰 에이전트 23건) 반영: 홈 공지는 고정된 '공지'만(고정된 모임 글은 다가오는 모임에서만)·소식 조회 100건·총 메시지 계산 통일(`totalOf`)·v1 통계는 홈에서 숨김·최신 발행본은 `order=updated_at.desc`로; 소식 모집 안내문(목록 줄에는 날짜·시간·장소만)·글감/후기 쓰기로 열 때 작성 중이던 글을 덮어쓰지 않음(안내 줄 + 그 글 불러오기)·`#write-prompt`는 인기 글보다 먼저·실패 시 해시 유지; 맛집 지도는 핀 구성이 바뀔 때만 다시 맞춤(검색·펼치기에 튀지 않음)·한줄평 창 방어·가까운 순 옵션이 사라지면 정렬 복귀·닫기 포커스 대체·`aria-controls`는 펼쳤을 때만·`#pick` 실패 안내; 통계 공유 링크에 최신이 아닌 발행본 ID(`?report=`)·발행 화면 알림 줄은 자료가 바뀌면 숨김·'기준 주' 안내는 집계 마지막 날 기준; 모집 마감 라벨(날짜 없는 글); 스타일시트 `site.css?v=8`. 반영하지 않은 것: 추천 메뉴 줄을 '없는 메뉴만'으로 바꾸자는 제안(문서 규칙 1.2-5와 기존 검사가 전체 나열이 맞음), 깊은 링크 글을 목록 맨 위에 두는 것(영향 작음), 추천 카드 포커스 이동(키보드 도달성이 더 중요).
- 2차 검토(정산·정산 확인·안내·SQL, 20건) 반영: 정산 확인은 NOT_IN_LIST/BAD_NAME 을 "명단에 없다 → 새 링크" 안내로, 같은 이름의 입금 표시 요청이 끝나기 전 재요청 차단; 정산은 이름 30자 통일(확인 화면·서버와 같은 길이), 카카오톡으로 보내기가 늘 다시 저장(같은 id), 링크 만들기 중복 실행 방지, 예시 데이터 때 영수증 초기화, 만료·삭제된 링크는 잊고 새로 발급, 복사 창 닫으면 포커스 복귀, 진행 단계에 aria-current·완료/진행 중 낭독, 뺀 멤버가 결제자였던 차수는 모임장 결제로; 이용 안내 검색이 주요 이용 규칙도 찾고 지우면 검색이 펼친 문답을 접으며 "절 N개 · 문답 M개"로 셈, 해시 이동 때 검색 해제, 공유 안내 문구; SQL — 이름 검사를 `site_features.sql`·`setup_all.sql` 에도 넣어 설치 파일을 다시 돌려도 남고, rows 가 배열이 아닌 payload 는 빈 명단으로(로컬 PostgreSQL 16 에서 명단 안/공백 이름/명단 밖/없는 정산/31자/rows null 여섯 경우 확인); 정산 확인·발행 화면의 상위 탭에서 `aria-current` 제거; 게임 실행 링크에 이름. 반영하지 않은 것: 결제 이미지 저장의 두 번 호출(Safari 글꼴 예열 — 주석으로 남김).
- 3차 검토(대화 파서·통계 데이터 흐름, 20건) 반영: 800일 상한은 최근 800일을 남기고 잘린 메시지를 합계에서도 빼서 메타·일별·멤버 합이 늘 같음, 병합 서명에 분(같은 시간대의 "ㅋㅋ"·"ㅇㅇ"가 섞이지 않게), 동명이인 번호(민지·민지2)를 원본 이름 사전순으로 고정해 발행 화면의 체크가 늘 같은 사람을 가리킴, PC 형식 입장·퇴장 줄은 직전 메시지 시각을 이어받아 나간 사람 감지가 됨, 날짜로 시작하는 본문 줄은 이어 붙임, '시작'과 '답'의 60분 경계 분리, 답한 적 없는 멤버는 replyMed null, 나간 사람·○○야 같은 부르는 꼴을 단어 목록에서 차단, 날짜를 모르는 맨 앞 시스템 줄은 세지 않음, 빈 결과도 같은 모양, 병합은 파일 등장 순서를 보존; 통계 화면 메타 줄에 잘림 안내, 새 멤버 문장은 첫 말을 꺼낸 새 멤버가 있을 때만 평균을 말함, 명단 붙여넣기 매칭 구분자를 파서와 통일. 검증: `parser7test.js` 11건(900일·병합·동명이인 제외·PC 퇴장·본문 날짜 줄·60분 경계·replyMed·이름 차단·날짜 전 시스템 줄·빈 결과). 반영하지 않은 것: 닉네임에 " : "가 든 경우, 활동 순위 120명 밖 멤버 제외, 옛 v1 발행본의 주 단위 계산, 빠른 기간 버튼의 빈 기간 안내, 닉네임의 " : " 처리 외 위 항목들. 예시 데이터(`data/report-sample.json`)에는 새 항목(starts·replyMed·textCount·newcomers·leaves·meta.truncated)을 기존 숫자에서 어림해 넣어 두어 예시 화면에서도 '대화의 문을 여는 사람'·'답이 오기까지' 문장이 보인다(합계·순위 숫자는 그대로).
- 4차 검토(소식·맛집 저장 흐름, 20건) 반영 15건: 위치 잡기가 (대개 운영진의) 비밀번호를 저장하던 것 제거, 새 글 닉네임은 저장된 내 닉네임만(수정 창에 남은 남의 이름 방지), 수정 저장이 작성 중 글의 임시 저장을 지우지 않음, 서버 형식 확인이 늦게 끝나도 열린 창의 입력값 유지, 다시 시도 뒤 `#write-prompt`·`#pick` 이어감, 후기 제목 100자·제목/내용 길이 검사, 링크로 먼저 불러온 글의 목록 중복 방지, 글 삭제 뒤 다음 페이지 시작점 보정, 이전 형식 서버에서 후기·정보·공지 안내문을 실제와 맞춤, 운영진 비밀번호 오류 문구(고정에도), 첫 한줄평 실패 이유 표시, 좌표 오류 문구(BAD_COORD), 삭제된 항목 문구; SQL — `place_update` 이름 겹침을 ALREADY_EXISTS 로(로컬 확인), `post_update` 는 종류가 바뀔 때 고정 여부를 따라감(공지로 → 고정, 공지에서 → 해제, 같은 종류면 유지 — 로컬 확인). 남긴 것: 댓글 입력 중 목록 다시 그리기(입력값 보존), 포커스 복원 키의 모호함, 댓글 불러오기 실패 표시, 미리보기의 링크 스킴 검사, 삭제 창 문구.
- 뺀 것(2026-09-26, 운영자 요청): 통계 화면의 '운영 인사이트' 절(문장 7종·카드 4종 — 시간대 비중·평일/주말·월별 비교·새 멤버 적응·2주 무발화·집중도·응답 시간, 대화 시작자·분위기 메이커·시간 유형·사진과 링크)을 화면·CSS·그리기 코드째 지움. 파서가 내는 starts·replyMed·media 같은 값은 발행본에 그대로 남고 멤버 상세(시간 유형)에서 계속 쓴다. 이용 안내 정산 참여자 단계의 '오류는 조용히' 항목도 설명 문장까지 지워 두 단계(내용 확인·신속히 입금)만 남김.
- 그림 적용(2026-09-21, 챗GPT 생성본 13장): 규격은 `docs/IMAGE_SPEC.md` 4~8절. 검증 — 기존 스위트 15개 전부 통과, 새 `imgtest.js`(3폭 표시·실패 시 접힘·넘침·배너 열 복귀) 통과, 스크립트 오류 없음. 독립 검토(관점 5 × 확인·반박 검증, 16건 → 10건 반영, 6건 반박됨): 배너 실패 시 그림 열이 남던 것을 `.page-head-grid.no-image` 로 한 열 복귀(인쇄도), 스팟·원판 테두리를 안쪽 그림자 대신 `border` 로(img 위에서는 안쪽 그림자가 안 보임), 고대비 테두리를 원판·오프라인 그림에도, 서비스 워커 설치가 offline.html 저장 실패를 삼키고 옛 저장본을 지우던 것을 설치 실패로 바꿈, 배너 sizes·문서 표시 크기(450×300 / 350×233)와 OG 문서 정정. 반박된 것: 파일 없는 자리의 404(의도된 동작), 오프라인 그림 1x 한 장(문서대로), 원판 480w 후보·스팟 sizes(동작 결함 아님), offline.html 머리 주석. 뒤이어 통계 스팟·정산 스팟·통계 빈 상태 3장(대화창 첨부 webp)과 통계·정산 OG 2장도 적용해 16장 전부 붙음.
- 직접 확인하지 못한 것: 실제 카카오톡 공유 창(SDK 는 t1.kakaocdn.net — 이 환경에서 차단, 도메인 등록은 완료)·실기기 위치 권한·홈 화면 설치는 배포 뒤 실기기에서 봐야 한다. 실서비스 데이터 표시는 프록시 차단으로 못 봄.
- SQL 실행 완료(운영자, 2026-09-26): `sql/2026-09-21-places-2-place-fn.sql` 재실행(함수 3줄 확인), `sql/2026-09-24-content-format-3-posts.sql` 재실행(`content_format 2`·`places_location 1` 확인), `sql/2026-09-26-settle-guard.sql`(`settle_set_paid` 1줄 확인). 콘텐츠 양식 파일은 붙여넣다 잘려서 세 쪽으로 나눴고, 1·3쪽은 위치 파일이 만든 뷰를 건드리지 않게 했다(로컬 PostgreSQL 에서 위치 파일 뒤 재실행·위치 파일 없는 서버·고정 규칙 5경우 확인). 배포 뒤 맛집 지도·공유·내 주변·홈 화면 추가를 실기기에서 한 번씩.

## 12. 전면 커버 — 그림이 화면을 채우고 글은 그 위에 (8차)

배포된 7차 그림 적용을 보고 운영자가 "그림 임팩트가 훨씬 커야 하고 글이 너무 많다"고 했다. 진단: 그림이 제목 옆 작은 상자에 갇혀 있었고 모든 화면이 글 목록으로 시작했다.

- 공통(`assets/site.css` v10 `.cover`): 홈형·배너형·스팟형 전면 커버. 그림이 화면 폭을 채우고(`object-fit: cover`, 초점 50% 42%) 제목·소개·버튼은 아래쪽 보라 그라데이션 위에 흰 글자. 커버 위 버튼은 흰 바탕. 실패 시 보라 그라데이션만. 인쇄·고대비 규칙. 규격 `docs/IMAGE_SPEC.md` 9절.
- 홈(`index.html`): 계절 그림이 첫 화면을 채움(PC ≥ 580px, 모바일 ≥ 520px). 주요 기능 4행 → 탭 그림 카드 4장(`.tab-cards`). 옛 히어로·기능 행 CSS 삭제.
- 안내·맛집: 배너 전면 커버(맛집은 PC 에서 등록 버튼 오른쪽 아래). 소식·통계·정산·정산 확인: 보라 띠 + 200px 그림 타일(모바일 112px). 소개 문장은 한 줄로 줄임. 통계의 출처 줄은 커버 아래로.
- 검증: `imgtest.js` 100건(커버 표시·높이·글 위치, 글자 대비 픽셀 측정 7페이지×3폭 최소 7.3:1, 빈 상태·뽑기판·오프라인·홈 카드, 실패 경로 그라데이션 복귀), `herocheck.js` 를 커버 구조로 다시 씀, `contenttest.js` 홈 검사 갱신, 기존 스위트 전부.
- 독립 검토(8건 + 제안 3건) 반영: 커버 위 초점 고리를 흰색으로, 홈 커버 아래 간격을 `.home-grid` 여백으로, 인쇄에서 커버 높이 고정·알약·버튼 해제와 회색 구분선, 스팟 실패 처리를 배너와 같은 방식(커버에 `.no-image`, 그림 열·간격 제거)으로 통일, 짧은 화면에서 홈 커버 높이를 `clamp(420px, 100vh − 140px, 580px)`, 그림 없는 홈 커버는 360/320px 로, 360px 이하 폰의 스팟 88px, 커버를 `header` 에서 `section` 으로(배너 랜드마크 중복 해소), 문서 4·5절의 옛 자리 설명 정리, 죽은 선택자 제거. 반영하지 않은 것: 소식 커버만 좁은 컨테이너(아래 게시판 폭과 맞추는 쪽을 택함), 1920 이상에서 1440px 그림 업스케일(원본이 1536 이라 더 큰 판이 없음), 홈 맛집 카드의 720px 배너(지연 로딩 80 KB, 허용).
- 검사 스크립트: `herocheck.js` 의 "로딩 전후 위치" 단계가 그림 요청을 붙잡아 둔 채 `document.fonts.ready` 를 기다려 1440 폭에서 멈추던 것(크로미움은 load 전에 fonts.ready 를 미룬다)을 시간 대기로 바꿈. `contenttest.js` 의 `.page-lead` 참조를 `.cover-lead` 로.
- 남긴 것: 본문 글 자체(안내 문서, 통계 절 설명)는 줄이지 않았다 — 머리를 그림으로 바꾸고 소개를 한 줄로 줄이는 데 집중. 더 줄이려면 절 설명을 접기(details)로 바꾸는 방법이 있다.

## 13. 2세대 그림 50장 반영 — 본문 미니 스팟 (2026-09-21 밤)

운영자가 `docs/IMAGE_PLAN.md` 8-6 의 지시서로 만든 50장을 ZIP 4개(`CLAUDE_HANDOFF.md`·`CLAUDE_PROMPT.txt`·매니페스트)로 보냈다. 매니페스트 경로·SHA-256 50/50 일치 확인 뒤 붙였다. 규격·자리 표는 `docs/IMAGE_SPEC.md` 6·10절.

- 파일: 원본 PNG 는 저장소에 넣지 않고(2.9 MB 급) 보존판 `assets/img/src/<이름>.webp`(품질 0.92, 투명 유지) 50장 + 웹용 100장. 1차와 같은 이름 16장(안내·맛집 배너, 소식·통계·정산·오프라인 스팟, 오늘 뭐 먹지, 빈 상태 3, 계절 6)은 같은 이름으로 교체해 자리 변경 없이 바뀜. 공유 미리보기 5장(`og-guide/places/news/report/settle.jpg`)은 새 그림으로 다시 조립(눈으로 글꼴 확인).
- 새 자리(기존 UI 가 있는 곳에만): 맛집 목록 행·홈 최근 맛집 카드의 종류 타일(`content.js` `CAT_IMG`, 8종 밖은 선 아이콘), 홈·안내 참여 단계 타일(`.flow > li.has-img`), 안내 7개 장 머리(정산 장은 정산 스팟), 소식 글쓰기 창의 글 종류 옆(후기는 기록 그림, 목록은 제목 중심 그대로), 정산 단계 제목 1~3·카톡 안내 상자(입력·결과 칸 무관), 통계 칭호 6종(나머지 칭호는 선 아이콘), `404.html`(새 파일 — Vercel 정적 404, noindex, robots 제외, `sw.js` 2026-09-21b). 빈 상태 그림은 2세대 원본 비율대로 4:3 → 3:2(자르지 않음).
- 실패 처리: 모든 타일은 `onerror` 로 선 아이콘·번호 원판·아이콘으로 복귀. 지연 로딩이라 아래쪽 타일은 스크롤해 가까워질 때 요청된다.
- 유지한 것: 맛집 검색·필터·정렬·초기화·상세(사례 4곳), 정산 계산·복사·공유, 통계 기간별 수치·히트맵, 안내 검색, 방문 카운터, 커버 구조, 계절 수동 교체(가을 유지). 입력 흐름은 바꾸지 않음.
- 검증: 새 `scratchpad/img2test.js` 27건(1440·390 — 타일 로드·크기·아이콘 숨김·실패 복귀·글 종류 전환·404·8종 파일 존재) 통과, `imgtest.js`(빈 상태 3:2) 통과, 기존 스위트 contenttest·places7test·guide7test·guidetest·news7test·calc7test·reporttest·a11ytest·a11y7test·uitest·visitstest·herocheck·settle-test 전부 통과, JS 오류 없음. 화면: `img2shots.js` 78장(1440·768·390 — 커버·단계·목록·장 머리·글쓰기 창·정산 단계·칭호·404·오프라인·실패 경로)과 뷰포트 캡처(홈 커버, 맛집 상세 유쾌한그집 선릉점, 소식 빈 상태). 옛 `placestest.js`·`newstest.js` 는 7차 이전 선택자(`.place-card`·`.post-card`)를 찾는 낡은 스크립트라 시간 초과 — `places7test`·`news7test` 가 대체.
- 미사용: 없음(50장 전부 자리 있음). 계절 3벌 6장은 파일만 갱신(자리는 가을).
- 직접 확인하지 못한 것: 배포 뒤 Vercel 이 `404.html` 을 404 상태로 내보내는지(정적 배포 규칙대로 두었음), 실서비스 데이터 화면(프록시 차단), 실기기 카카오톡 미리보기 갱신(캐시가 남을 수 있음).
- 공유 미리보기 6장 커버형으로 교체(머지 뒤, 운영자 지적 "오픈채팅방에 뜨는 이미지도 업데이트"): 옛 카드는 크림 바탕에 글 왼쪽·그림 오른쪽 타일이라 새 사이트(전면 커버)와 얼굴이 달랐다. 홈·안내·맛집은 그림이 카드를 채우고 보라 그라데이션 위 흰 글자(대비 13.6~16.4:1 측정), 소식·통계·정산은 보라 띠 + 크림 타일. 원본 `assets/img/src/og*.html` 6개를 다시 쓰고 `scratchpad/mkog3.js` 로 캡처·검사(글꼴·그림·글자 10~90%·≤250 KB·대비). `og:image` 와 SDK 기본 그림에 `?v=2`(`site-core.js?v=7`). 규격 `docs/IMAGE_SPEC.md` 2절. 카카오톡이 페이지 주소별로 미리보기를 기억하므로 바로 바꾸려면 카카오 디벨로퍼스 공유 디버거로 새로 고침.

## 14. 2세대 그림 재배치 — 작은 자리에서 빼고 큰 자리로 (2026-09-22, 9차)

운영자가 배포된 13절 결과를 보고 "항목별 그림이 조화롭지 못하고 겉돌며 조잡하다"고 했다. 진단(34장을 캔버스에 올려 물체 차지 비율·밝기를 잼): 문제는 그림이 아니라 자리 — 스팟 아트를 44~56px 아이콘 자리에 넣어 물체가 15~25px 로 보였고(정산 4장은 정사각의 9~33%만 차지), 한 줄 안에 밤 장면(밝기 0.30)과 크림 물건(0.6)이 섞였으며, 칭호 목록은 주황 메달과 보라 선 아이콘이 번갈아 나왔다. 큰 자리(커버·빈 상태·홈 카드·404)는 문제없었다. 운영자가 안 1(작은 자리에서 빼고 큰 자리에만)을 택함.

- 뺀 곳(선 아이콘·번호 원판 복귀): 맛집 목록 29줄 타일, 홈 최근 맛집 카드 타일, 홈 참여 단계 4칸, 안내 장 제목 옆 56px, 정산 단계 제목 1~3·카톡 안내 상자, 소식 글쓰기 창의 종류 옆, 통계 칭호 목록의 40px 메달. `site.css` v12 의 `.has-img`·`.mini-img` 블록 삭제.
- 옮긴 곳(80px 이상, 규격 `docs/IMAGE_SPEC.md` 10절): 음식 8 → 맛집 상세 머리(`.pd-hero` 128/96px + 요약 칩); 단계 4 → 안내 '처음 2주' 카드 4장(`.step-cards`, 그림 4:3 + 번호 배지, PC 4열·폰 2×2); 규칙 6 + 정산 스팟 → 안내 각 장의 여는 줄(`.cat-open` 128/96px + 첫 항목); 정산 4 → 정산 진행 stepper 카드(`.stepper.is-cards` 80px, 넓은 화면만 — 폰은 점 stepper 그대로라 입력 칸이 밀리지 않음); 글 종류 4 → 소식 이번 주 글감 카드(96/80px, 글감 종류대로)와 종류별 빈 상태(120px); 메달 6 → 통계 메달 카드(`#badgeCards` 88/72px, 투명 메달), 나머지 6개 칭호는 목록에 선 아이콘. 34장 전부 자리 있음.
- 캐시: 같은 이름으로 교체한 9종(안내·맛집 배너, 소식·통계·정산 스팟, 오늘 뭐 먹지, 빈 상태 3)의 참조 40곳에 `?v=2`. 오프라인 그림은 서비스 워커 판 번호가 맡는다. `site.css?v=13`.
- 검증: `img2test.js` 를 새 자리 기준으로 다시 씀(31건 — 목록에 그림 없음, 상세 머리·카드·장 삽화·글감·빈 상태·stepper·메달 카드의 크기·로드·실패 복귀·`?v=2`) 통과. `imgtest.js` 는 그림 차단 패턴이 `?v=2` 를 못 잡던 것을 정규식으로 고쳐 100건 통과. contenttest 100·places7test 24·guide7test 7·guidetest 6·news7test 19·calc7test 12·reporttest 15·a11ytest 24·a11y7test 14·uitest 33·visitstest 8·herocheck 14·settle-test 9 통과, JS 오류 없음. 화면: `img3shots.js` 15자리 × 1440·768·390 + 맛집 상세 머리(술집·한식) 눈으로 확인.
- 고친 것: 단계 카드 그림이 `height` 속성 때문에 세로로 늘어나던 것(`height: auto`). 정산 stepper 는 예시 데이터가 실려 있으면 4단계 모두 '완료'라 진행 중 표시가 없는데, 이는 원래 동작.
- 남긴 것: 정산 stepper 그림은 폰에서 보이지 않음(입력 칸을 밀지 않기 위해). 배포 화면·카톡 안 브라우저 캐시는 직접 보지 못함.

## 15. 전체 감사와 최적화 (2026-09-22, 10차)

운영자 요청("전체적으로 검토해서 안 맞거나 부족하거나 오류가 있거나 모바일·데스크탑에서 문제가 있는지 최적화"). `scratchpad/audit9.js` 로 11개 페이지(홈·안내·맛집·소식·통계·정산·정산 확인·발행·게임·오프라인·404) × 6폭(360·390·768·1024·1440·1920)에서 JS 오류·콘솔 오류·로컬 404·가로 넘침·좁은 화면 탭 크기·라벨 없는 입력·alt 없는 그림·깨진 그림·중복 id·제목 단계·내부 링크·메타·아래 고정 바 겹침을 재고, 390 에서 API 실패(500) 상태 화면을 확인했다. 모달 5종(맛집 등록·한줄평·정보 수정·소식 글쓰기·비밀번호 확인)은 `modals10.js` 로 390·1440 캡처.

- 결과: JS 오류·로컬 404·가로 넘침·깨진 그림·중복 id·앵커 없음·파일 없음 — 전 페이지·전 폭 0. API 실패 시 홈·맛집·소식·통계는 안내 문구와 '다시 시도'가 뜸(안내·정산·게임은 서버 자료를 안 씀). 모달은 폰에서 바닥 시트(68px 아래 전부)로 스크롤되고 초점이 안에 머무름.
- 고친 것 ① 접근성 — 정산의 차수 이름·금액 입력 4개에 `aria-label`(라벨 없던 입력), 발행 화면의 파일 입력에 `aria-label`, 맛집·소식 목록 앞에 숨긴 `h2`(h1 → h3 로 건너뛰던 제목 단계).
- 고친 것 ② 손가락 크기 — 정산 멤버 칩의 × 버튼 22 → 32px, 맛집 종류 칩 30 → 34px(768px 이하), 발행 화면의 '지난 제목 그대로 쓰기'(인라인 스타일 24px 제거) 와 접기 제목 32px, 404 의 로고·메뉴 링크 32px.
- 고친 것 ③ 성능 — 정산·통계의 `html-to-image`(그림 저장용 외부 스크립트)가 `<head>` 에서 렌더링을 막던 것을 `defer` 로; 정산·정산 확인에 `preconnect`(jsdelivr) 추가.
- 고친 것 ④ 폰 브라우저 — 홈 커버 높이의 `100vh` 에 `100svh` 를 덧대어 주소창을 뺀 높이로(모르는 브라우저는 vh 유지).
- 고친 것 ⑤ 검색·공유 — 공개 7페이지에 `rel="canonical"`, `sitemap.xml` 생성과 `robots.txt` 의 `Sitemap:` 줄.
- 감사가 잡았지만 문제 아님: 홈 최근 맛집 카드의 `a.stretch`(카드 전체가 눌림), 발행 화면의 `sr-only` 파일 입력(버튼이 대신 누름), 푸터가 탭 바 아래로 73px 들어감(푸터 아래 여백 84px 이라 글자는 안 가려짐), 통계·정산의 콘솔 오류(이 환경의 프록시가 jsdelivr 를 막음), 오프라인·404 의 description 없음(noindex), 정산 확인 `?id=demo` 의 '찾을 수 없음'(주소 형식이 실제와 달라서 — 실제 흐름은 settle-test 9건).
- 검증: 스위트 17개 전부 통과(img2test 31·imgtest 100·contenttest 100·places7test 24·guide7test 7·guidetest 6·news7test 19·calc7test 12·reporttest 15·a11ytest 24·a11y7test 14·uitest 33·visitstest 8·herocheck 14·settle-test 9·admin7test 4·parser7test 11), 감사 재실행으로 라벨·제목 단계·탭 크기 항목 해소 확인, 바꾼 버튼 크기 실측(`tapcheck.js`).
- 남긴 것: 배포 화면·실기기(iOS 사파리·카톡 브라우저)는 직접 못 봄. 게임 3페이지(`game.html`·`game_v2.html`·`bakery.html`)는 별도 앱이라 이번 감사 범위 밖.

## 16. 실기기 레이아웃 재검토 — 주요 기능 카드 잘림과 iOS 보정 (2026-09-22, 11차)

운영자가 폰(카톡 안 브라우저)에서 홈 '주요 기능' 카드의 그림이 잘리거나 칸을 넘는 것을 보고, 전체 레이아웃의 부정확·어색·잘못된 배치를 검토해 달라고 했다. 크로미움 헤드리스에서는 멀쩡했던 것이라 실기기(iOS 사파리·WKWebView) 차이를 중심으로 봤다. 8개 관점(iOS 호환·폰 폭·태블릿 폭·PC 폭·시각 일관성·그림 자리·입력 양식과 모달·데이터 상태별)의 병렬 감사를 돌려 80여 건을 모았고, 크레딧 사정으로 검증 단계 초반에 멈춘 뒤 근거가 분명하고 고치기 싼 것부터 반영했다.

- **원인(주요 기능 카드)**: `.tc-img` 가 `aspect-ratio` 상자 + 자식 `img { height: 100% }` 구조였는데, iOS 사파리는 그 상자의 높이를 percentage 기준으로 잡지 못해 그림이 자연 높이(정사각)로 커지고 상자에 잘렸다. padding 으로 비율을 잡고(`::before { padding-top: 62.5% }`) 그림을 절대 위치로 채우는 구조로 바꿈(contain 유지). 못 읽으면 크림 상자 대신 글만.
- **iOS 폴백**: `dvh` 앞에 `vh`(본문 최소 높이·모달·바닥 시트), 히트맵 칸 padding 정사각, 소식 알림 위치를 `:has()` 없이도 떠 있는 버튼 위로.
- **iOS 초점 확대(검증 확정)**: 손가락 화면에서 폼 컨트롤 글자 16px 미만이면 iOS 가 초점 때 화면을 확대하고 되돌리지 않는다 — 안내 검색 15px, 소식 정렬 14px, 맛집 필터 15px, 통계 select, 정산 추가·차감 14px. `@media (hover: none) and (pointer: coarse)` 에서 input·select·textarea 를 16px 로 고정.
- **iOS select·date 모양(검증 확정)**: `select.input` 과 date·time 입력이 iOS 기본 menulist 모양이라 옆 칸(44px·8px 모서리)과 높이가 달랐다 → appearance 를 끄고 우리 테두리 + SVG 화살표. (검색 칸의 알약 모양 주장은 검증에서 기각 — iOS 15~17 은 작성자 테두리를 유지.)
- **긴 닉네임 넘침**: `html { overflow-wrap: anywhere }`(넘칠 때만 끊음) + 정산 영수증 입금 명단의 이름을 말줄임(바깥 span 이 줄어들도록) — 한 사람 이름 때문에 전원의 금액·복사 버튼이 카드 밖으로 밀리던 것. 요약 타일 금액은 100만 원(7자리)도 들어가게 글자 크기 clamp.
- **폰에서 잘려 안 보이던 것**: 소식 종류 탭(자유)은 480px 이하에서 두 줄로, 맛집 종류 칩 3개(술집·카페·분식)는 줄바꿈으로(스크롤 단서가 없었다). 히트맵 '한 화면' 규칙을 480 → 640px 까지.
- **중간·넓은 폭**: 상단 메뉴가 861~999px 에서 두 줄로 꺾이던 것(부제 숨김·탭 여백 축소), 홈 최근 맛집 카드 3장이 534~787px 에서 2열이 되어 하나가 홀로 남던 것(1열), 안내·맛집 배너가 1280~1920 에서 380px 로만 잘려 인물 머리가 잘리던 것(`clamp(380px, 27vw, 540px)`), 정산 결과 열이 화면보다 길어 아래 버튼이 안 보이던 것(열 안에서 스크롤).
- 잔재 정리: 정산·발행 입력 초점 테두리의 옛 초록색 → 보라, 정산 stepper 그림의 `loading="lazy"` 제거(첫 화면).
- 검증: 바꾼 자리 실측(소식 탭 2줄·전부 보임, 맛집 칩 2줄·넘침 0, 상단 메뉴 900·980 한 줄, 배너 1440 389px·1920 518px, 정산 결과 열 max-height 812px, select 16px·appearance none), 스위트 재실행.
- **반영하지 않고 남긴 것(감사에서 나왔으나 비용·위험 대비 뒤로)**: 통계 시간대 차트 축 글자가 861~1024px 에서 작음(JS 글자 크기 계산), 핵심 요약 타일 10개가 641~1023 에서 3열(홀로 남는 타일), 안내 검색 결과 수 문구가 목차 안에 숨음, 소식·맛집·홈이 자료를 받으면 목록이 아래로 뛰는 것(자리 예약 필요), 정산기만 가변 폰트 한 파일(2.1MB)을 받는 것, iOS 에서 서버 저장 뒤 클립보드 쓰기가 거부될 수 있는 것(ClipboardItem 방식 필요), 카톡 안 브라우저의 이미지 저장 실패 안내, 발행 화면 좌우 열 높이 불균형, 정산 확인·정산기·발행의 옛 녹색 팔레트 잔재와 버튼·안내 상자 모양 통일, 320px 폭 넘침, 페이지별 푸터 위 여백 차이.

## 17. 이용 안내 재구성 — 상황별 빠른 답 + 도해 장 (2026-09-23, 12차)

운영자 지적: 이용 안내에 중복이 많고 글이 많아 읽기 어렵다(필요한 내용은 지우지 말 것), 그림으로 부각하고 빨리 이해되게. 먼저 설계 워크플로로 사실 116개(필수 98)와 중복 지도(닉네임 예시 5번, 중간 합류 5번, 2개월 4번, 참석 버튼 4번 …)를 뽑고, 재구성안 3개를 두 심사가 채점해 'C 상황별 빠른 답'(84점)을 뼈대로 다른 안의 좋은 점을 붙인 명세를 만들었다.

- **구조**: 커버 → '무엇이 궁금하세요?' 질문 타일 8장(그림 + 질문 + 굵은 답 조각, PC 4열·폰 2열) → 장 8개(처음 2주 / 닉네임과 재입장 / 모임 가기 / 모임 비용 정산 / 모임 열기(새 장) / 연락과 대화 / 금지 행동 / 사이트 사용). 각 장 머리는 타일과 같은 그림 + 번호·제목 + 핵심 한 줄. 옛 '주요 이용 규칙' 6줄·'처음 2주 안내' 판·장 여는 줄은 없앴다.
- **사실은 한 곳에만**: 같은 규칙을 여러 번 쓰던 문장을 모두 한 자리로 모았다(검사: '엑서 논현 남 88 0125'·'2개월에 1회 이상'·'참석 버튼으로 신청'·'1/N이 원칙'·'운영진 닉네임을 눌러'·'먼저 연락하면 가능'·'모임 정산 열기'·'공지 없이 따로 모이는 모임' 각 1번). 규칙 문답 11개는 본문과 같은 말이라 지우고, 목차 '자주 찾는 질문' 5줄이 그 자리로 이동만 시킨다. 필수 사실 98개는 모두 남았다(`scratchpad/review/guide11/facts.json` 대조, 누락 0). 뺀 것은 이유·배경 문장 9개(닉네임의 목적, 재입장이 기록에 주는 영향, 임의 참여가 정산을 어긋나게 한다 등)와 장 제목 옆 대상 표시뿐.
- **도해**: 처음 2주 장면 카드 3장 + 정기 참여 띠, 닉네임 5칸(한 줄) + 좋은 예·나쁜 예 이름표 + 숫자 카드(입장 후 1회 · 최대 1회 · 최대 3회), 참석 3단계 흐름 + '달라지면 모임장에게' 연락표(바로 / 먼저 연락하면 가능 배지), 정산 역할 레인 2개(그림·단계·칩) + 도구 카드, 원칙·예외 카드, 만드는 법 탭 경로, 운영진 연락표 + 오픈채팅 버튼, 메시지 삭제 대비, 금지 칩 판, 조치 단계 비교(금지 행동 위반 2단계 대 비벙 관련 3단계를 차수 칸으로 정렬).
- **이동·검색**: 타일은 기록을 쌓아 폰에서 뒤로 가기로 타일 격자에 돌아온다. `#start`(1장 장면 줄)·`#cat-*`·하위 자리 16곳(`#rule-dup` 은 문답을 펼침)으로 들어오면 그 자리로 스크롤·포커스. 검색은 장 → 블록 → 문답 단위로 거르고, 검색 결과 수를 목차 밖(검색칸 아래)에 둬 폰에서도 보인다(11차 남은 항목 해소). 검색 대상에서 파일 이름·앵커 값은 뺐다('webp'·'rule-' 로는 안 걸림).
- **수치**(`.guide-doc` 기준, 같은 환경): 폰 390 페이지 10,043 → 6,646px(−34%), PC 1440 7,614 → 5,701px(−25%), 보이는 본문 글자 약 3,700 → 2,332자(−37%). 첫 화면(390×844)에 타일 4장. 가로 넘침 0(320~1440), 그림 21장 모두 80px 이상, 모든 그림 실패 시 21곳 글만 남음.
- 폰 커버 높이를 이 페이지만 200px 로 낮췄다(첫 화면에 타일이 보이게). 키가 낮은 PC 창(≤860px)에서는 옆 목차 칸만 따로 스크롤.
- **독립 검토(3관점) 반영**: 닉네임 변경 카드 '입장 후 1회' → '입장 후 1회만'(횟수 제한이 한 곳에만 남아 뜻이 분명해야 함), '확정되면 누르기' → '참여가 확정되면 누르기', 현장 단계의 같은 말 반복 정리('공지한 시간을 지키고, 안내한 방식에 맞춥니다'). 목차 표시를 위치 기준으로 다시 계산(하위 자리로 이동한 뒤 다음 장이 표시되던 것, 방향을 바꾸면 멈추던 것), 처음 들어올 때 글꼴이 늦게 도착해 #자리가 화면 위로 지나치던 것(글꼴·본문 높이가 바뀌면 사용자가 움직이기 전까지 다시 맞춤), 키 낮은 PC 창(1366×768·1280×720)에서 목차 마지막 줄이 잘리던 것(줄 높이 축소), 320px 정산 레인 글 칸 118px(그림을 위로), 소제목 7개를 h3 로(화면 읽기 제목 이동), 경로·조치 칸의 '›' 를 읽지 않게, 넓은 화면의 누를 수 없는 '목차' 머리를 탭 순서에서 뺌, 검색어 보강(시간 약속·질문·FAQ·예외 상황·카톡·배려).
- 검사: `scratchpad/guide7test.js`(구조·검색 13가지·주소 진입 4·타일→뒤로 가기·폰 목차), `img2test.js`(안내 그림 자리·크기·실패 경로), `g12/check.js`(필수 사실·중복·3폭), 나머지 스위트 재실행.

## 18. 정밀 재검토 — 이용 안내·통계 발행 (2026-09-23, 13차)

운영자 지적: 결과물마다 오류나 이슈가 남는다, 정밀하게 다시 보라. 그리고 활동 통계를 새 대화 파일로 올리려는데 운영진 항목이 안 보인다. 4관점(안내 내용·안내 화면·안내 동작·통계 발행 흐름)으로 찾고, 지적마다 다른 검토자가 처음부터 다시 재현해 확인된 것만 남겼다(49건 중 확인 40·부분 확인 8·기각 1). 모두 고치고, 검토자가 남긴 재현 스크립트로 다시 돌려 고쳐진 것을 확인했다.

- **운영진 발행 페이지**: `report-admin.html`. 9/19 노출 축소 때 공개 페이지의 링크를 모두 빼서 찾을 길이 없었다 → 활동 통계 맨 아래에 작은 글 링크(비밀번호 보호·검색 제외는 그대로).
- **통계 발행(데이터가 틀리던 것)**: ① 나감 감지된 사람을 다시 체크해도 발행본에서 빠져 있던 것 ② 앞 토막이 같은 두 사람(민지 강남·민지 송파) 중 남아 있는 사람을 빼고 나간 사람을 넣던 것 — 원본 닉네임을 파서와 같은 규칙의 표시 이름(민지·민지2) 하나로만 다루게 바꿈(`ChatParser.displayNames`, `excludeDisplay`) ③ 이모지로 시작하는 닉네임을 가리면 반쪽 글자가 되어 서버가 발행을 거부하던 것(글자 단위로 자름) ④ PC 내보내기에서 그날 첫 줄의 입장·퇴장이 전날 마지막 시각을 받아 나간 사람 감지·환영 집계가 빠지던 것 ⑤ PC 공지 속 날짜 한 줄('2026.9.13')이 뒤 메시지들을 그 날짜로 옮기던 것 ⑥ 빠른 기간(이번 달·지난달·최근 60일)이 파일 밖·미래 날짜를 0으로 채워 하루 평균을 낮추던 것(파일 날짜 안으로 자름, 파일 밖 날이 들어가면 안내) ⑦ 공개 통계의 '신규 멤버'가 첫날 말하지 않은 사람을 모두 새 멤버로 세던 것(입장 기록이 있으면 입장 기록 기준) ⑧ 명단 적용 뒤 '제외 N명'이 두 배로 나오던 것 ⑨ 괄호·밑줄 닉네임('도윤[서초]', '[서초]도윤')이 동네를 싣던 것 ⑩ 같은 대화를 iPhone·PC 로 함께 올리면 입장이 두 번 세지던 것.
- **통계 발행(안내·편의)**: 비밀번호를 아직 정하지 않았을 때의 안내, 서버 한도(약 1MB)를 넘을 만한 기간이면 미리 알림·실패 문구에 '기간을 줄여' 안내, 메시지 없는 기간을 고르면 이유와 '전체 기간으로' 버튼, 중복 정리 수를 메시지와 입장·퇴장 기록으로 나눔, 삭제 뒤 적던 명단을 저장된 것으로 덮던 것, 굵은 글이 줄을 끊던 것, 점검 줄의 '기준 주' 안내가 한 번도 나오지 않던 것(목록에서 집계 기간을 함께 받음), '있나·계세·먹을까' 같은 동사 조각 키워드, 체크 하나에 1년치 파일을 두 번 집계하던 것(제외 전 집계 캐시·주차 키워드 한 번 훑기: 3.2초 → 1.4초, 계산 중 표시).
- **이용 안내 내용**: 참여자 3단계 '입금 완료'는 모임장이 링크를 올렸을 때만(조건), 5장에 '자격: 첫 모임에 1회 참여한 뒤부터' 줄(→ 처음 2주), 7장에 '다른 장의 금지'(임의 참여·중복 개설) 이동 줄, 재입장 예외는 '운영진에게' 미리 알림, 바로가기 안내를 안드로이드로 한정, 정리 날짜 9월 23일, 검색어 보강(닉변·강퇴·N빵·번개·방장·메세지·출생 연도 등).
- **이용 안내 동작**: 한글 입력 중 낱자에서 '결과 없음'이 깜빡이던 것(입력 대기·조합 끝에 거름), 띄어쓰기·문장부호가 다르면 못 찾던 것('재 입장'·'닉네임변경'·'1 / N'·'정산?' — 한글 사이 띄어쓰기만 무시해 '1:1'이 '1981 1982'에 걸리지 않게), 장 제목으로 찾으면 그 장의 핵심 블록이 숨던 것('금지'·'처음 2주'), '원칙·예외' 배지가 검색되지 않던 것, 검색 뒤 이동이 접히는 문답 때문에 지나치던 것, 페이지 끝에서 목차가 8장을 표시하지 못하던 것, 검색 중 목차 표시·흐림을 화면 읽기에도 알림, Ctrl·Cmd 클릭(새 탭)을 가로채던 것, 검색으로 글이 줄면 검색칸이 위 메뉴 뒤로 숨던 것.
- **화면(여러 페이지 공통, site.css v17)**: 방문 수 알약이 뜨면 로고가 두세 줄로 꺾이고 메뉴가 한 글자씩 꺾이던 것(320~359px 는 알약을 빼고 861~1023px 는 두 줄 알약), 문답 머리의 키보드 초점 테두리가 잘려 안 보이던 것, 키보드 초점이 위 메뉴·아래 탭바 뒤로 숨던 것(scroll-padding — 페이지별 scroll-margin 은 그만큼 줄여 이동 자리는 전과 같음), 아이폰 가로 화면의 노치 쪽 여백.
- **화면(이용 안내)**: 넓은 화면 배너에서 손님 얼굴이 잘리던 것(초점 위로), 1장 장면 그림 세 장의 크림 테두리 모양이 제각각이던 것(원본에서 그림 부분만 잘라 다시 만듦, ?v=2), 이동한 자리에 옮긴 포커스가 아이폰에서 '선택된 카드'처럼 보이던 테두리, 좁은 폭에서 '때'·'사이' 한 글자 줄.
- **남긴 것(운영진 결정 필요)**: 임의 참여·중복 개설에 어떤 조치 단계를 적용하는지. (운영진 3명을 알아보는 법은 운영자 확인 뒤 반영 — 6장 핵심 줄 '닉네임 옆에 방장·부방장 표시가 있는 3명', 2장 닉네임 변경 카드에서 그 줄로 이동 링크) 커버 그림은 원본이 1536px 라 1920 모니터·레티나에서 조금 흐리다(새 그림이 있어야 함).
- 검사: `scratchpad/guide7test.js`(55항목), `parser13test.js`(파서 8항목), 검토자 재현 스크립트(`g13/stats-publish/t1·t2·t3·t5·t6·t7·t9·t10`, `g13/verify-guide-behavior/f2·f3·f5·f6·f12`), 전체 스위트.

## 19. 운영진 발행 페이지 비밀번호 확인 화면 (2026-09-23)

운영자 요청: 운영진 파트에 들어갈 때 비밀번호를 요구해 달라.

- `report-admin.html` 을 열면 '운영진 확인' 화면만 보이고, 비밀번호가 맞아야 발행 도구가 열린다. 확인 전에는 발행 목록도 불러오지 않는다.
- 비밀번호 판정은 서버에서만 한다(브라우저에 정답이나 해시를 두지 않음). 새 SQL 없이 기존 `report_delete(p_pass, -1)` 을 쓴다. 이 함수는 비밀번호부터 확인하고(틀리면 BAD_PASS), id 는 1부터 매겨지므로 -1 은 없는 번호라 아무것도 지우지 않는다.
- 확인한 비밀번호는 발행·삭제 칸에 미리 채우고 입력칸은 숨긴다. 저장하지 않으므로 새로고침·'잠그기'를 누르면 다시 묻는다. 5번 틀리면 30초 동안 확인하지 않는다(화면 쪽 보조 장치, 실제 보호는 서버 비밀번호).
- 서버에 닿지 못하거나 서버 설정이 없으면 열지 않고 안내한다. 도중에 비밀번호가 바뀌어 발행이 거부되면 발행 칸에 비밀번호 입력을 다시 보여 준다.
- 참고: 정적 사이트라 페이지 파일 자체는 누구나 받을 수 있다. 발행·삭제는 원래부터 서버가 비밀번호를 확인하므로, 이 화면은 도구를 가리는 문이고 데이터 보호는 서버 쪽이 맡는다.
- 검사: `scratchpad/gatetest.js`(390·1440, 틀림·맞음·잠그기·5회 제한·서버 끊김·설정 없음, 비밀번호 저장 안 함), `admin7test.js` 는 확인 화면을 통과한 뒤 진행.

## 20. 이용 안내 정렬, 줄바꿈, 레이아웃 정밀 정리 (2026-09-23, 15차)

운영자 지적: 이용 안내 탭의 레이아웃, 글자 정렬, 줄바꿈이 들쑥날쑥하고 조잡해 보인다. 320, 360, 390, 430, 768, 960, 1024, 1440 폭에서 캡처하고, 네 관점(정렬, 줄바꿈, 글자 체계와 간격, 컴포넌트 일관성)으로 측정해 찾은 50건을 한 번에 정리했다. 내용(사실 98개)은 바꾸지 않았다.

- **글자 체계 한 벌**: 장 제목(--fs-title), 블록 제목 16px 700(만드는 법, 메시지 삭제, 조치 단계와 카드 제목이 세 가지 모양이던 것), 읽는 글 15px 1.6, 카드 글 14px 1.55, 보조 글 13px, 라벨 13px 700, 태그 12px 700. 10.5px, 12.5px 같은 한 번만 쓰는 크기를 없앴다. 굵은 글은 한 가지(700, 본문 색)로, 보라 강조는 장 머리 한 줄과 정기 참여 띠에만.
- **카드 안쪽 여백 한 벌**: 가로 PC 16px, 폰 14px(테두리 있는 카드는 1px 빼서 글 왼쪽 선이 모두 같게). 예전에는 카드마다 9에서 19px 로 달라 카드를 내려가며 글 시작선이 흔들렸다. 모서리도 10px 로 맞춤.
- **장 머리**: 여덟 장 모두 그림, 번호와 제목, 핵심 문장의 같은 구조(1장에 그림과 문장, 8장에 문장을 더함). 핵심 문장이 두 문장이면 문장마다 새 줄. 폰에서는 그림과 제목을 한 줄에 두고 핵심 문장은 아래 전체 폭(그림 옆 좁은 칸에서 서너 줄로 접히던 것).
- **폰에서 들쑥날쑥하던 블록**: 만드는 법은 번호 세로 목록(화살표만 줄 첫머리에 남던 것), 금지 목록은 칩 더미 대신 한 줄씩(PC 두 단), 예외 칩은 같은 폭 줄, 정산 레인은 그림과 제목을 한 줄에 두고 단계는 전체 폭, 숫자 카드 세 장은 같은 구조, 메시지 삭제 대비는 한 칸씩.
- **정렬**: 조치 단계 칸마다 1차, 2차 글자 높이가 달랐던 것(위에서부터), 빈 점선 칸 제거, 단계 연결선이 원에서 떨어져 있던 것(원 아래에서 다음 원까지, site.css 의 .flow 라 홈 참여 흐름도 같이 고침), 두 줄 이름표나 두 줄 항목 옆 라벨과 배지가 줄 사이에 뜨던 것, 닉네임 다섯 칸이 PC 에서 왼쪽에만 몰리던 것, 320px 에서 닉네임 칸 오른쪽 빈자리, 960px 미만에서 검색칸과 목차가 본문보다 넓던 것.
- **줄바꿈**: 문서 전체에 text-wrap: pretty, 정기 참여 띠는 balance. 붙어 있어야 하는 말(1:1 대화, 참석 버튼, 입금 완료, 카카오톡으로 보내기, 임시 저장, 홈 화면에 추가 등)은 데이터에서 줄바꿈 없는 공백으로 묶었다(검색 글에서는 보통 공백으로 바뀜). 문장 안 링크 앞 두 칸 공백과 링크 줄만 높아지던 것, 자주 묻는 질문의 점 목록(이름 칸과 설명 칸으로), PC 에서 한 줄 60자가 넘던 답 글(34em 제한), 320px 질문 타일 제목이 두 줄로 밀리던 것.
- **콜아웃**: 비벙 안내가 혼자 왼쪽 줄무늬 모양이던 것을 다른 콜아웃과 같은 판, 같은 아이콘 칸으로.
- 옆 목차가 생기는 폭(960px)은 그대로 두었다. 1080px 로 올려 보면 1024px 에서 본문 오른쪽이 250px 가까이 비어서, 그 폭에서 좁아지던 정산 레인은 새 배치로만 해결했다.
- site.css v18(연결선 규칙), 모든 페이지 판 번호를 올림.
- 검사: `scratchpad/guide7test.js`(55항목, 320px 정산 레인 항목은 새 배치 기준으로 고침), `g12/check.js`(사실 98개, 중복, 3폭), `img2test.js`(장 머리 8, 그림 22장), `uitest.js`, `a11y7test.js`, `herocheck.js`, 폭별 넘침과 칸 밖 글자 측정(`g15/me/m1.js`, `w.js`, 320에서 1440까지 0건).
