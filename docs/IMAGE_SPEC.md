# 이미지 규격 (내부 문서)

이미지는 별도로 제작합니다. 사이트는 이미지 없이도 완성된 상태이며, 아래 자리에 파일이 있을 때만 그 자리가 열립니다. 이미지 개수에 맞춰 섹션을 만들지 않습니다. 자리는 홈 대표 그림·공유 미리보기와, 2026-09 에 더한 탭별 머리 배너·머리 스팟·빈 상태 그림·오늘 뭐 먹지·오프라인입니다(4~8절). 기획과 생성 프롬프트는 `docs/IMAGE_PLAN.md`.

공통 규칙(4~8절): 모든 자리는 `<img … onerror>` 로 붙어 파일이 없거나 못 읽으면 자리째 접히고 글·버튼만 남는다(빈 상자·깨진 아이콘 없음). 원본은 `assets/img/src/<이름>.webp`(생성 크기 그대로, 품질 0.92), 웹용은 `assets/img/<이름>-<폭>.webp` 두 단계(1x·2x)를 srcset·sizes 로 고른다. 줄이기는 `scratchpad/mkimg2.js`(크로미움 캔버스, 가운데 자르기 → 두 배씩 줄이기). 장식 그림은 `alt=""`, 장면 그림은 "…그린 일러스트" 대체 텍스트. 인쇄에서는 전부 숨김, 고대비 모드에서는 테두리 한 줄.

색 기준: 페이지 바탕 `#F7F5EF`, 흰 면 `#FFFFFF`, 살짝 눌린 면 `#EDE9EA`, 브랜드 보라 `#4B2A82`, 글자 `#241E2C`, 보라와 짝인 녹슨 주황 `#A9502D`·황토 `#7E5F25`(라벨·차트에만). 그림의 선명한 주황·빨강은 그림 안에만 둡니다.

## 1. 홈 대표 그림 (적용됨 — 가을)

브랜드 일러스트입니다. 실제 멤버나 실제 현장 사진으로 소개하지 않습니다(대체 텍스트도 "…그린 일러스트").

| 항목 | 값 |
|---|---|
| 원본 | `assets/img/src/autumn-desktop.webp` 1536 × 1024 (3:2, 354 KB) · `assets/img/src/autumn-mobile.webp` 1122 × 1402 (4:5, 339 KB). 원본은 손대지 않음 |
| 웹용 파일 | PC `assets/img/autumn-desktop-1440.webp` 1440 × 960 (273 KB), `autumn-desktop-720.webp` 720 × 480 (99 KB) · 모바일 `assets/img/autumn-mobile-800.webp` 800 × 1000 (197 KB), `autumn-mobile-400.webp` 400 × 500 (68 KB). 크로미움 캔버스로 줄이고 WebP 품질 0.8~0.82 로 저장. 파일 서명(RIFF/WEBP, VP8X)과 크기를 헤더에서 확인함(`scratchpad/webpinfo.js`) |
| 고르는 규칙 | `<picture>`: 767px 이하 → 세로형(400w/800w), 768px 이상 → 가로형(720w/1440w). 화면 배율(1x/2x/3x)에 맞는 파일은 브라우저가 srcset·sizes 로 고름 |
| PC 표시 (≥ 768px) | 글(제목·소개·버튼)과 나란히. 글 38% · 그림 62% (1440px 화면에서 그림 630 × 420). 비율 3:2 그대로, 잘라내지 않음 |
| 모바일 표시 (≤ 767px) | 제목·소개·버튼 다음에 세로형. 화면 폭 전체로 늘리지 않고 340px 까지, 가운데(390px 화면에서 340 × 425). 가로형과 비교했더니 가로형은 350 × 233 에 얼굴 폭이 20px 남짓이라 세로형을 채택 |
| 잘림 | 없음. width/height 속성과 CSS aspect-ratio(3/2, 4/5)가 파일 비율과 같아 얼굴·장면이 그대로 보임 |
| 로딩 | 속성으로 자리를 먼저 잡아 로딩 전후 제목·아래 섹션 위치가 움직이지 않음(측정 0px). 로딩 중 바탕은 `#EEECE4` |
| 실패 | 파일을 못 읽으면 `img` 의 onerror 가 `.no-image` 를 붙여 그림 열이 사라지고 글만 한 열로 남음. 빈 상자 없음 |
| 모서리·효과 | 모서리 10px 뿐. 그림자·테두리·글자 겹침 없음 |
| 주변 배경 | 페이지 바탕 `#F7F5EF` |
| 대체 텍스트 | "가을 단풍과 호수를 배경으로 모닥불 곁에 둘러앉아 이야기를 나누는 다섯 사람을 그린 일러스트" |
| 바꾸는 방법 | `index.html` 의 "계절 대표 소개" 블록 하나에 제목(h1)·소개(p)·파일 경로(picture)가 모여 있음. 계절이 바뀌면 그 블록만 고침. 자동 전환 없음 |

## 2. 공유 미리보기 (OG) 이미지 (적용됨 · 2026-09-21 커버형으로 교체)

카카오톡·메신저에 링크를 붙이면 뜨는 카드 그림. 8차 전면 커버(9절)와 같은 모양으로 다시 만들었다 — 링크를 눌러 들어갔을 때 보는 첫 화면과 같은 얼굴.

| 항목 | 값 |
|---|---|
| 파일 | 1200 × 630 JPG 품질 92, 전부 ≤ 200 KB. 홈·게임 목록 → `assets/og.jpg`(197 KB), 안내 → `og-guide.jpg`(193 KB), 맛집 → `og-places.jpg`(187 KB), 소식 → `og-news.jpg`(66 KB), 통계 → `og-report.jpg`(85 KB), 정산·정산 확인 → `og-settle.jpg`(81 KB). 발행 화면에는 og:image 가 없음 |
| 원본 | `assets/img/src/og.html`·`og-<탭>.html` — 이 페이지를 1200 × 630, 배율 1 로 캡처. 다시 만들 때는 `scratchpad/mkog3.js`(글꼴·그림 로드·글자 가로 10~90%·크기·흰 글자 대비를 함께 잰다) |
| 배너형(홈·안내·맛집) | 그림(홈은 가을 PC 3:2, 안내·맛집은 배너)이 카드를 채우고(`object-fit: cover`, 초점 50% 42%) 아래 42% 부터 짙어지는 보라 그라데이션(사이트 `.cover::after` 와 같은 값) 위에 흰 글자: 알약(오픈채팅 커뮤니티 · 2026 가을 / 동네친구들 · 오픈채팅 커뮤니티) · 제목 88px · 소개 30px · 기능 나열 21px. 글자 범위의 가장 밝은 픽셀 기준 흰 글자 대비 13.6~16.4:1 |
| 스팟형(소식·통계·정산) | 사이트 스팟형 커버와 같은 보라 그라데이션 띠(`135deg #2E1B52 → #4B2A82 → #6D4AAE`) + 오른쪽 위 흰 빛, 왼쪽 흰 글자, 오른쪽 380px 크림 타일(모서리 92px, 흰 고리) 안에 스팟 그림 |
| 안전 영역 | 글자 왼쪽 120px(10%)부터, 오른쪽 끝 ≤ 1080px(90%). 스팟 타일 오른쪽 끝 1080px. 카카오톡이 좌우를 조금 잘라도 글자·그림이 남음 |
| 문구 규칙 | 각 탭 커버의 소개 문장과 같게. 홈은 계절 블록과 같이 — 계절을 바꾸면 `og.html` 의 그림 파일(`autumn-desktop.webp` → 다른 계절 PC 판)과 알약 문구를 바꾸고 다시 캡처 |
| 캐시 | `og:image` 주소와 카카오톡 공유 SDK 의 기본 그림(`site-core.js`)에 `?v=2` 를 붙여 그림 캐시를 새로 받게 함. 카카오톡은 **페이지 주소 단위로도** 미리보기를 한동안 기억하므로, 바로 바뀌게 하려면 카카오 디벨로퍼스의 공유 디버거(https://developers.kakao.com/tool/debugger/sharing)에 주소를 넣어 캐시를 새로 고친다. 그림을 다시 만들 때마다 `?v=` 숫자를 올린다 |

## 3. 게임 목록 화면 캡처 (선택, 자리 아직 없음)

| 항목 | 값 |
|---|---|
| 파일 | `assets/img/game-zombie.webp`, `assets/img/game-bakery.webp` |
| 권장 원본 | 1280 × 720 px, 16:9 |
| PC 표시 | 각 행 오른쪽, 240 × 135 px (실행 버튼 옆) |
| 모바일 표시 | 제목 아래 화면 폭 전체, 350 × 197 px |
| 잘림 | `object-fit: cover`, 가운데 기준 |
| 주변 배경 | 흰 패널 `#FFFFFF`, 모서리 8px |
| 비고 | 실제 게임 화면 캡처만. 파일이 들어오면 홈과 같은 방식(picture + onerror)으로 games.html 에 붙임 |

## 4. 머리 배너 — 이용 안내·맛집 (파일 규격. 자리는 9절 전면 커버로 바뀜, 2026-09-21)

| 항목 | 값 |
|---|---|
| 파일 | 안내 `assets/img/guide-banner-1440.webp`(214 KB)·`-720.webp`(80 KB), 맛집 `assets/img/places-banner-1440.webp`(212 KB)·`-720.webp`(79 KB). 원본 `assets/img/src/guide-banner.webp`·`places-banner.webp` 1536 × 1024 |
| 자리·표시·실패 | 9절 전면 커버(`.cover` + `img.cover-img`, `sizes="100vw"`). 옛 자리(제목 옆 42% 열, 잘라내지 않음)는 8차에서 없앰 |
| 대체 텍스트 | 안내 "식당 문 앞에 도착한 사람을 안에서 반기는 친구들을 그린 일러스트" · 맛집 "해 질 무렵 식당이 늘어선 골목을 걷는 친구들을 그린 일러스트" |

## 5. 머리 스팟 — 소식·활동 통계·모임 정산·정산 확인 (파일 규격. 자리는 9절 스팟형 커버로 바뀜, 2026-09-21)

| 항목 | 값 |
|---|---|
| 파일 | 소식 `assets/img/news-spot-480.webp`(16 KB)·`-240.webp`(6 KB), 통계 `report-spot-480/240.webp`(13/5 KB), 정산·정산 확인 공용 `settle-spot-480/240.webp`(15/6 KB). 원본 1254 × 1254(통계·정산은 대화창 첨부로 받은 webp 를 그대로 보존) |
| 자리·표시·실패 | 9절 스팟형 커버(`.cover.is-spot` + `img.cover-spot` 200px, 모바일 112px, 360px 이하 88px). 옛 자리(제목 왼쪽 104px 타일)는 8차에서 없앰 |
| 대체 텍스트 | `""` (장식 — 제목이 뜻을 전한다) |

## 6. 빈 상태 그림 — 소식·맛집·활동 통계·404 (적용됨, 2026-09-21 · 2세대로 교체)

| 항목 | 값 |
|---|---|
| 파일 | 소식 `assets/img/news-empty-640.webp`(35 KB)·`-320.webp`(12 KB), 맛집 `places-empty-640/320.webp`(41/14 KB), 통계 `report-empty-640/320.webp`(20/7 KB), 404 `notfound-empty-640/320.webp`(51/15 KB). 2세대 원본 1536 × 1024(3:2)를 **자르지 않고** 640 × 427 / 320 × 213 으로 줄임(1세대는 4:3 으로 잘라 썼음) |
| 자리 | 소식 "등록된 글이 없습니다"·맛집 "등록된 맛집이 없습니다"(`.empty-state` 맨 위), 통계 "불러오지 못했습니다"(`.status-box` 맨 위), 404 안내 카드(`404.html`, 10절). 검색 결과 없음·종류 필터 없음에는 두지 않음 |
| 표시 | 240px 폭(화면이 좁으면 그 폭), **3:2**(`aspect-ratio: 3 / 2`, `width="640" height="427"`), 모서리 12px, `loading="lazy"` |
| 실패 | `.no-image` 로 숨김. 소식은 원래 아이콘(확성기)이, 404 는 돋보기 아이콘이 다시 나옴 |

## 7. 오늘 뭐 먹지 카드·오프라인 페이지 (적용됨)

| 항목 | 값 |
|---|---|
| 오늘 뭐 먹지 | `assets/img/places-pick-240.webp`(13 KB)·`-480.webp`(40 KB, 2세대). 카드 왼쪽 64px 원판(크림 배경 + 녹슨 주황 테두리). 못 읽으면 숨고 종류 아이콘(`#pickIc`)이 나옴 |
| 오프라인 | `assets/img/offline-spot-240.webp`(3 KB)·`-480.webp`(7 KB, 2세대). `offline.html` 의 아이콘 자리에 150px. 서비스 워커(`sw.js`, VERSION 2026-09-21b)가 offline.html 과 함께 저장하고 그 요청만 저장본 우선으로 돌려줌. 못 읽으면 원래 아이콘 |

## 8. 홈 계절 그림 — 겨울·봄·여름 (파일 준비됨, 교체는 수동)

| 항목 | 값 |
|---|---|
| 파일 | `assets/img/home-{winter,spring,summer}-desktop-1440/720.webp`(201~237 KB / 78~92 KB), `home-{…}-mobile-800/400.webp`(160~176 KB / 59~64 KB). 원본 `assets/img/src/home-*-desktop.webp` 1536 × 1024, `home-*-mobile.webp` 1024 × 1536(웹용은 가운데 1024 × 1280 을 잘라 4:5) |
| 교체 | `index.html` 계절 블록의 알약 문구·h1·소개·source·img(+alt) 다섯 곳과 `assets/og.jpg`(`og.html` 의 그림·문구). 문구 후보와 대체 텍스트는 `docs/IMAGE_PLAN.md` 4-1. 자동 전환은 두지 않음(가을 유지 중) |

## 9. 전면 커버 — 모든 탭의 머리 (적용됨, 2026-09-21 · 8차)

4절 배너와 5절 스팟의 "자리"를 이것으로 바꿨다(파일은 그대로). 그림이 화면 폭을 채우고 제목은 그 위에 흰 글자로 올라간다. 규칙은 `assets/site.css` v10 블록.

| 항목 | 값 |
|---|---|
| 세 가지 | **홈형**(`.cover.is-home`): 계절 그림. PC 3:2·모바일 4:5 파일을 `<picture>` 로 고르고 `object-fit: cover` 로 채움. 높이 PC ≥ 580 / 모바일 ≥ 520. **배너형**(`.cover`, 안내·맛집): 배너 3:2 를 채움. 높이 ≥ 380 / 300. **스팟형**(`.cover.is-spot`, 소식·통계·정산·정산 확인): 짙은 보라 그라데이션 띠 + 오른쪽에 200px(모바일 112px) 크림 타일 그림. 높이 ≥ 250 / 200 |
| 잘라냄 | 배너·계절 그림은 잘린다(1절의 "잘라내지 않음" 규칙은 홈 옆그림 시절의 것). 초점 `object-position: 50% 42%` — 인물이 가운데·위쪽이라 얼굴은 남고 아래쪽 바닥이 글 뒤로 들어간다 |
| 글자 대비 | 아래 42% 에서 급히 짙어지는 보라 그라데이션(맨 아래 92%) + 왼쪽 아래 타원형 어둠 한 겹. 홈은 타원을 약하게(왼쪽 인물 얼굴 보존). `scratchpad/imgtest.js` 가 글을 숨긴 캡처에서 제목·소개 글자 범위의 가장 밝은 픽셀과 흰색의 대비를 재며, 7개 페이지 × 3폭 모두 ≥ 7.3:1 (기준 4.5) |
| 글 | 알약(kicker) · 제목 46px(홈 58, 모바일 34/40) · 소개 한 줄 · 버튼(커버 위에서는 흰 바탕 보라 글자). 맛집은 넓은 화면에서 등록 버튼을 오른쪽 아래로 |
| 실패 | `onerror` 가 커버에 `.no-image` → 그림과 그라데이션이 빠지고 보라 그라데이션 바탕 위에 글만. 스팟은 그림만 사라짐 |
| 인쇄·고대비 | 인쇄: 그림·타일 숨기고 검정 글자. 강제 색상: 그림을 빼고 배경색 위 글자 |
| 홈 그림 카드 | 주요 기능 4행을 그림 카드 4장으로(`.tab-cards`): 맛집은 배너를 16:10 으로 채우고, 소식·정산·통계는 스팟을 크림 타일 안에 contain. 그림을 못 읽으면 크림 타일만 남고 이름·설명은 그대로 |

## 10. 2세대 본문 그림 — 큰 자리에만 (적용됨, 2026-09-22 · 9차 재배치)

`docs/IMAGE_PLAN.md` 7·8절 지시서로 만든 2세대 세트. 커버·스팟·빈 상태·계절 16장은 같은 이름으로 **교체**했고(4~9절의 자리 그대로, 참조에 `?v=2`), 아래 34장은 본문 안의 큰 자리(80px 이상)에 들어갔다. 처음(9-21 밤)에는 44~56px 아이콘 자리에 넣었는데 얼룩·빈 크림 네모로 보여 하루 뒤 전부 빼고 다시 놓았다(`REDESIGN_CHECKLIST.md` 14절). 원본 PNG(1254 × 1254 정사각·1536 × 1024 장면, 매니페스트 SHA-256 50/50 일치)는 저장소에 넣지 않고 보존판 `assets/img/src/<이름>.webp`(품질 0.92, 투명 유지)로 두었다. 공통 규칙은 `assets/site.css` v13 블록(`.spot-img`·`.step-cards`·`.cat-open`·`.stepper.is-cards`·`.empty-spot`).

**원칙**: 그림은 카드·머리·빈 상태에서 80px 이상으로만. 목록 줄·제목 옆·입력 칸 옆의 44~56px 자리에는 선 아이콘(`assets/icons.js`)만 둔다. 그림을 못 읽으면 `onerror` 가 그림만 지우고(자리에 `no-image`) 글·아이콘·번호는 그대로.

| 세트 | 파일(웹용) | 자리 | 크기 | 실패 |
|---|---|---|---|---|
| 음식 종류 8 | `food-{meat,korean,japanese,chinese,western,pub,cafe,bunsik}-160/320.webp` | 맛집 **상세 머리**(`.pd-hero` — 그림 왼쪽, 요약 칩 오른쪽). 종류 → 파일은 `assets/content.js` 의 `CAT_IMG`·`catImg()`. 목록 줄·홈 카드 타일은 선 아이콘 | PC 128 · 폰 96px | 칩이 전체 폭으로 |
| 참여 단계 4 + 정기 참여 1 | `flow-{enter,first-meeting,host,record}`(안내 장면용 `-480` 추가. 13차에 세 장 모두 원본에서 크림 여백을 걷어 낸 정사각으로 다시 만듦 — 곧은 여백·타원·찢긴 가장자리가 섞여 보이던 것, 참조 `?v=2`), `participation-spot` | 이용 안내 1장 **장면 3장**(`ol#start.step-cards.is-3`: 어두운 장면화 `flow-enter·first-meeting·host` 만 한 줄. 16차부터 카드 상자 없이 그림 + 글. PC·태블릿 3열 윗그림 4:3, 폰은 한 줄씩 88px 정사각 + 글)과 따로 떨어진 **정기 참여**(`.rule-band`, 16차부터 판 없이 `participation-spot` PC 104 · 폰 88px + 한 문장). 홈 '처음 참여한다면'은 선 아이콘 원판. `flow-record` 는 소식 글감·빈 상태의 '후기'용 | 카드 폭 · 88px | 번호·글만 |
| 안내 타일·장 머리 7 | `rules-{eligibility,nickname,meetup,manners,boundaries}`, `settle-spot`(정산), `posts-meetup`(모임 열기). `rules-faq` 는 18차에 사이트 사용 장을 빼면서 이용 안내에서 쓰지 않음(원판은 남김) | 이용 안내 **장 타일 7장**(`.qt`, '한눈에 보기' — PC·태블릿 88px, 16차부터 폰은 글만)과 같은 파일로 **장 머리**(`.cat-head .ch-art`, PC 112 · 폰 88px). 머리 그림은 2·3·5·6·7장만(1장은 장면 그림, 4장은 역할 그림이 바로 아래에 있어서), 그래서 폰에서는 한 그림이 한 번씩만 보인다. 4장 정산 레인에 역할 그림 한 쌍 `calc-rounds`(모임장)·`calc-payment`(참여자) 80px. 크림 그림은 틀·그림자 없이 가장자리를 바탕에 녹인다(mask, 17차부터 3%, 자른 판은 11절). 한 묶음 안에 어두운 장면화와 크림 스팟을 섞지 않는다(12차, `REDESIGN_CHECKLIST.md` 17절, 21절) | 80~112px | 글이 전체 폭으로 |
| 글 종류 4 | `posts-{notice,meetup,info,free}` (후기는 `flow-record`) | 소식 **이번 주 글감 카드**(`#promptArt`, 글감의 종류대로)와 **종류별 빈 상태**("정보 글이 없습니다" 위 120px, `.empty-spot`). 글쓰기 창의 종류 선택 옆·목록 줄에는 없음 | 96/80px · 120px | 전구 아이콘 / 문장만 |
| 정산 단계 3 + 공유 1 | `calc-{members,rounds,payment,share}` | 정산 **진행 stepper 카드**(`.stepper.is-cards`, 모임과 멤버 → 차수 금액 → 입금 정보 → 결과 공유) — 넓은 화면(≥ 768px)에서만 그림, 좁은 화면은 점 stepper 그대로. 단계 제목·카톡 안내 상자에는 없음 | 80px | 글만 |
| 칭호 메달 6 | `badge-{active,growth,guardian,welcome,sharing,reaction}-128/256.webp`(투명) | 통계 **메달 카드**(`#badgeCards`, 활약왕·성장왕·모임 지킴이·환영요정·정보공유왕·리액션왕). 나머지 칭호(새벽반·아침반·퇴근반·나들이 대장·맛집 탐험가·꾸준함)는 아래 목록에 선 아이콘 | PC 88 · 폰 72px, `contain` | 선 아이콘 |
| 404 1 | `notfound-empty-640/320.webp` | `404.html`(정적 배포에서 Vercel 이 404 상태로 내보냄, `noindex`, robots 제외) | 300px 3:2 | 돋보기 아이콘 |

미사용: 없음(34장 전부 자리 있음). `posts-notice`·`posts-meetup` 는 공지·모집 글감이나 그 종류의 빈 상태에서만 나온다. 검사는 `scratchpad/img2test.js`(1440·390, 자리·크기·실패 경로·`?v=2`), `imgtest.js`(커버·빈 상태 3:2).

## 11. 이용 안내 그림 — 그린 부분에 맞춰 자른 판 (적용됨, 2026-09-24 · 17차)

이용 안내의 크림 그림 10장(18차에 faq 판을 지움)은 `assets/img/guide/<이름>-<폭>.webp` 의 **자른 판**을 쓴다(`guide.html` 의 `pic()`). 원판(`assets/img/<이름>-*.webp`)은 정사각 캔버스에 그림이 놓인 자리와 여백이 제각각이라(원판 여백 2~14%), 같은 88px 칸 안에서 그림이 2~12px 들어가 앉고 그린 부분의 크기가 56~76px 로 들쭉날쭉했다. 다른 페이지(정산, 소식, 홈)는 원판을 그대로 쓰므로 원판은 바꾸지 않았다.

- 만드는 법: 보존판 `assets/img/src/<이름>.webp`(1254px)에서 가장자리 12px 띠의 중앙값을 바탕색으로 잡고, 바탕과 차이(RGB 합) 30 넘는 곳의 테두리 상자를 찾아 긴 변의 3% 여백을 더해 자른다. 바탕과 거의 같은 곳(차이 6 이하)은 페이지 바탕 `#F7F5EF` 로 맞추고 30 까지 서서히 원래 색으로(그림마다 바탕이 1~3 달라 옅은 네모가 비치던 것). 폭 160/320(`-spot` 은 240/480), 높이는 비율대로, 품질 82.
- 비율(폭/높이): nickname 1.023, meetup 1.007, posts-meetup 1.398, manners 0.96, boundaries 0.799, calc-rounds 1.04, calc-payment 1.534, eligibility 1.544, settle-spot 1.057, participation-spot 0.972. `pic()` 의 `RATIO` 표와 같다.
- 칸 안 배치: 칸(88 또는 112px 정사각)은 그대로 두고 `object-fit: contain`, 장 머리와 레인과 띠는 `object-position: left center`(그림이 글 쪽 선에 붙는다), PC 질문 타일만 가운데. 가장자리 녹이기(mask)는 5% → 3%.
- `participation-spot`(정기 참여)은 그림 자체에 칠한 네모 테두리가 있어, 자른 뒤 둥근 네모 마스크(안쪽 7%, 흐림 5%)로 가장자리를 바탕에 녹였다. 세 장의 어두운 장면화 바로 아래에서 네 번째 장면처럼 보이던 것.
- 파일 이름이 새 폴더라 캐시 꼬리표(`?v=`)는 없다. 그림을 바꾸면 이 절의 방법으로 다시 만들고 비율 표를 고친다.

## 만들지 않는 자리

- 맛집 식당 사진: 사진 첨부 기능이 없어 자리를 만들지 않음. 첨부 기능이 생기면 목록 행 왼쪽 96 × 96 (1:1), 상세 480 × 320 (3:2) 로 정함.
- 소식 글 이미지: 첨부 기능 없음. 사진은 오픈채팅에서 오감.
- 아이콘·일러스트: 기존 선 아이콘(`assets/icons.js`)으로 충분. 위 자리(4~10절) 밖의 장식 그림은 두지 않음(차트·표 옆, 콜아웃 안, 입력 칸·결과 카드, 소식 목록 행).
